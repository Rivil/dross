[verify]
  phase = "lane-selector-preview"
  generated_at = 2026-09-02T11:01:53.083097Z
  verdict = "fail"
  finalized = true
  finalized_at = 2026-09-02T12:22:34.650184Z

[summary]
  mutation_status = "measured"
  measured_on = "helicon"
  mutation_score = 0.9835390946502057
  mutants_killed = 239
  mutants_survived = 4
  mutants_not_covered = 4
  mutants_in_scope = 243
  unclassified_in_scope = 3
  criteria_total = 6
  criteria_covered = 5
  criteria_uncovered = 1

  [[summary.leg]]
    language = "go"
    tool = "gremlins"
    killed = 239
    survived = 4
    timeout = 0
    in_scope = 243
    score = 0.9835390946502057
    file_count = 17

[[criterion]]
  id = "c-1"
  status = "covered"
  tests = ["internal/cmd/lane_plan_test.go:39:TestPlanLineIsTheSpawnedLine", "internal/cmd/lane_preview_test.go:71:TestPreviewLineIsTheLineTheGateWouldSpawn", "internal/cmd/lane_preview_test.go:42:TestPreviewSpawnsNothing", "internal/cmd/lane_preview_test.go:250:TestPreviewTakesTrailingPathsAsFiles"]
  notes = "The same-code-path guarantee is asserted as string IDENTITY between lanePlan's derived Line and the line the gate actually spawned (captured by installSpawnRecorder), not each against a literal — the only assertion shape that cannot pass while preview and gate disagree. Spawn-freedom is asserted against the spawn/ssh/rsync recorders rather than against absent output, so a preview that ran the prepare silently would still fail."

[[criterion]]
  id = "c-2"
  status = "covered"
  tests = ["internal/cmd/lane_preview_test.go:126:TestPreviewNamesEveryNonRunningOutcome", "internal/cmd/lane_plan_test.go:71:TestLaneRunLineNamesDroppedPaths", "internal/cmd/lane_plan_test.go:97:TestEverySelectorPathGoneScopesToNothing", "internal/cmd/lane_plan_test.go:118:TestResolveReportsOutOfTreeAsData", "internal/cmd/lane_plan_test.go:189:TestPlanCarriesUnmatchedPaths"]
  notes = "All four non-running outcomes are exercised in ONE invocation, each asserted to carry its reason string ('not on disk', 'no declared lane matches', 'outside this repository') plus the scoped-to-nothing lane line. The single-invocation shape catches the real failure mode — a finding rendered only when it is the sole thing wrong."

[[criterion]]
  id = "c-3"
  status = "covered"
  tests = ["internal/cmd/lane_preview_consent_test.go:25:TestPreviewRendersAnUngrantedLaneWithoutRefusing", "internal/cmd/lane_preview_consent_test.go:56:TestStaleAndGrantedAreDistinguished"]
  notes = "Asserted as a PAIR against the gate over the same argv: preview prints 'consent: <absent>' beside the derived line and exits 0 while `dross test --files` exits exitLaneRefused. The consent ladder (stale vs absent vs granted) is asserted through ConsentState.String() so a second vocabulary cannot be hand-written in the test."

[[criterion]]
  id = "c-4"
  status = "covered"
  tests = ["internal/cmd/test_lane_list_fields_test.go:42:TestLaneListPrintsTemplateFields", "internal/cmd/test_lane_list_fields_test.go:68:TestListOmitsTemplateFieldsForALaneWithout", "internal/cmd/test_lane_list_fields_test.go:107:TestListAndAddRenderTemplatesIdentically"]
  notes = "Both template shapes covered (repeat {path} and joined {paths}), plus the absence case that keeps the fields opt-in. The identity test compares lane list's transcript against lane add's rather than each against a literal, which is what fails on a list-local second renderer."

[[criterion]]
  id = "c-5"
  status = "covered"
  tests = ["internal/cmd/lane_preview_json_test.go:62:TestPreviewJSONIsBareAndCarriesTheFindings", "internal/cmd/lane_preview_json_test.go:156:TestJSONCarriesConsentAndLocality", "internal/cmd/lane_preview_json_test.go:126:TestPreviewJSONReportsAnEmptyMatchWithoutFailing", "internal/cmd/lane_preview_json_test.go:103:TestPreviewJSONUsesItsOwnFlagUsage"]
  notes = "Every field the criterion names is asserted: lanes, line, dropped, unmatched, out_of_tree, consent, locality, host_state. Bareness is checked via assertBareJSONDocument (no '# <path>' header). The payload's strings are compared against the TRANSCRIPT, not against literals, so a second vocabulary in the JSON fails. The empty-match case pins `lanes` to an array rather than null and exit 0."

[[criterion]]
  id = "c-6"
  status = "weak"
  tests = ["internal/cmd/lane_preview_locality_test.go:71:TestUnreachableHostIsUnresolvedNotAFailure", "internal/cmd/lane_preview_locality_test.go:125:TestMissingRemoteToolFallsBackNamingTheBinary", "internal/cmd/lane_preview_locality_test.go:224:TestUnresolvedHostNeverRefusesALane", "internal/cmd/lane_preview_locality_test.go:261:TestUnprobedHostNeverClaimsLocal", "internal/cmd/lane_preview_locality_test.go:190:TestPreviewLandsOnTheGrantTheRunWouldUse", "internal/cmd/lane_preview_consent_test.go:102:TestUnreachableHostStillExitsZero"]
  notes = "The granted-host, local-fallback-naming-the-binary and unprobed halves are covered well. The 'host that cannot be reached is reported as unresolved rather than turning preview into a failure' half is only HALF covered: preview has two unresolved paths and the tests reach one. preflightRemote converts remote.ErrTransport into Fallback=true with a nil error, so every fakeProbe in the suite lands on the `chosen == nil` branch at lane_preview_locality.go:127. The `perr != nil` branch at :119 — a host that answered and whose answer was a failure (non-transport probe error) — is executed by no test, which is why three in-hunk mutants survive there NOT_COVERED. That is the branch where propagating the error is the natural instinct, i.e. exactly the regression c-6 forbids."

[[finding]]
  severity = "BLOCKING"
  text = "gremlins mutant survived: internal/cmd/lane_preview_locality.go:119 (INVERT_NEGATIVES) — flips the `perr != nil` guard on preview's 'host answered with a failure' path; no test reaches the branch, so nothing notices preview turning a probe failure into a returned error"

[[finding]]
  severity = "BLOCKING"
  text = "gremlins mutant survived: internal/cmd/lane_preview_locality.go:119 (ARITHMETIC_BASE) — mutates `targets[len(targets)-1]`; nothing asserts which host preview names when a probe fails non-transport"

[[finding]]
  severity = "BLOCKING"
  text = "gremlins mutant survived: internal/cmd/lane_preview_locality.go:119 (ARITHMETIC_BASE) — second index mutation on the same unreached expression"

[[finding]]
  severity = "FLAG"
  text = "criterion c-6 downgraded covered -> weak: the unresolved-host promise is proved for the transport-failure path only; the non-transport probe-failure path (lane_preview_locality.go:113-119) has no covering test"

[[finding]]
  severity = "NOTE"
  text = "survivor accepted this run: internal/cmd/lane_preview.go:249 (CONDITIONALS_NEGATION) — gremlins attributes no coverage block to a `case X != \"\":` switch condition. Proven exercised: go coverage reports block 249.24,253.64 count=1 under TestMalformedLaneIsAFindingNotAFault, which asserts the exact string that branch prints. Tooling attribution ceiling, not a test gap."

[[finding]]
  severity = "NOTE"
  text = "retired 2 stale acceptances whose subject mutants are no longer produced (internal/cmd/verify.go: 0896f0c77e9a1e27, 74e811ee04b389a7)"

[[finding]]
  severity = "NOTE"
  text = "mutation measured on helicon: score 0.98 over 243 in-scope mutants (killed=239, survived=4, all 4 NOT_COVERED); efficacy over the 239 reachable = 1.00. 111 out-of-scope survivors filtered — see `out_of_scope` in tests.json."
