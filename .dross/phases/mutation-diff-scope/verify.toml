[verify]
  phase = "mutation-diff-scope"
  generated_at = 2026-08-09T17:09:19.417143Z
  verdict = "pass"
  finalized = true
  finalized_at = 2026-08-09T17:30:46.820225Z

[summary]
  mutation_status = "measured"
  mutation_score = 0.9328621908127208
  mutants_killed = 264
  mutants_survived = 19
  mutants_not_covered = 19
  mutants_in_scope = 283
  criteria_total = 7
  criteria_covered = 7
  criteria_uncovered = 0

[[criterion]]
  id = "c-1"
  status = "covered"
  tests = ["internal/verify/verify_test.go:TestFilterReportDropsUntouchedSibling", "internal/cmd/verify_scoping_test.go:TestScopingAttributionHoldsEndToEnd", "internal/cmd/verify_test.go:TestVerifyScopesToPhaseChangesEndToEnd"]
  notes = "End-to-end over a real temp git repo: the untouched sibling's survivor lands only under out_of_scope and never in languages[].mutation.surviving. No surviving mutant in FilterReport itself."

[[criterion]]
  id = "c-2"
  status = "covered"
  tests = ["internal/verify/verify_test.go:TestFilterReportCounterImmunity", "internal/verify/verify_test.go:TestFilterReportConcreteArithmetic", "internal/verify/verify_test.go:TestSkeletonScoresFromFilteredReport", "internal/mutation/gremlins_test.go:TestParseGremlinsJSONPerFileNoDrift"]
  notes = "Numerator and denominator immunity are asserted separately, so pruning only the Surviving slice fails; TestFilterReportConcreteArithmetic pins 0.50 over 2 rather than 0.91 over 11. No surviving mutant in the recompute."

[[criterion]]
  id = "c-3"
  status = "covered"
  tests = ["internal/verify/verify_test.go:TestRunScopedPersistsOutOfScopeSurvivors", "internal/verify/verify_test.go:TestSkeletonOneNoteNoFlagFlood", "internal/verify/verify_test.go:TestRunScopedKeepsLanguageOnDroppedSurvivors", "internal/cmd/verify_test.go:TestVerifyPrintsOutOfScopeStatusAndSampleSize"]
  notes = "Presence in out_of_scope and absence from the per-language lists are both asserted; the NOTE/FLAG counts are asserted as counts, so a per-survivor loop over the unfiltered set fails. This run exercised the path for real: 89 out-of-scope survivors persisted, one count NOTE emitted."

[[criterion]]
  id = "c-4"
  status = "covered"
  tests = ["internal/verify/verify_test.go:TestFilterReportIsAdapterAgnostic", "internal/mutation/stryker_net_test.go:TestStrykerNetRePrefixParityWithStryker", "internal/mutation/gremlins_test.go:TestGremlinsRunRePrefixesPackagePaths", "internal/mutation/stryker_test.go:TestStrykerRePrefixMovesPerFileKeys"]
  notes = "One logical mutant set through a gremlins-parsed and a stryker-parsed report yields identical kept and dropped counts, and the same table runs inside a two-leg RunScoped. The inherited survivors in stryker.go/stryker_net.go are pre-existing adapter I/O paths, not the path-shape normalisation this criterion rests on."

[[criterion]]
  id = "c-5"
  status = "covered"
  tests = ["internal/verify/verify_test.go:TestSkeletonOutOfScopeStatus", "internal/cmd/verify_scoping_test.go:TestScopingVacuousPassIsNotReachable", "internal/cmd/verify_test.go:TestVerifyPrintsOutOfScopeStatusAndSampleSize"]
  notes = "The status is asserted against all three wrong answers (measured, unmeasurable, bare 0.00), and a genuinely clean run over the same phase is proved to read as measured — so the signal distinguishes. This run's own status is measured over 283 in-scope mutants, the non-vacuous side."

[[criterion]]
  id = "c-6"
  status = "covered"
  tests = ["internal/verify/verify_test.go:TestTestsScopeRoundTrip", "internal/verify/verify_test.go:TestTestsDegradedScopeIsReadableAfterTheFact", "internal/cmd/verifyscope_test.go:TestPhaseScopeRecordsResolvedSha", "internal/cmd/verifyscope_test.go:TestPhaseScopeSourceProvenance", "internal/cmd/verifyscope_test.go:TestPhaseScopeFencesBaseRef", "internal/cmd/verify_test.go:TestVerifyCover_ScopeFileListTruncation", "internal/cmd/verify_test.go:TestVerifyCover_ScopeFileListAtCapIsNotTruncated"]
  notes = "The round-trip asserts the reload, not just the write, so a missing json tag fails; the base is pinned as the resolved sha (c434f37) rather than the ref. t-6 closed the last gap: the CLI's display truncation now has both the over-cap overflow count and the at-cap boundary pinned, and both mutants that survived the previous run on that branch are killed here."

[[criterion]]
  id = "c-7"
  status = "covered"
  tests = ["internal/verify/verify_test.go:TestFilterReportTagsEveryKeptSurvivor", "internal/verify/scope_test.go:TestScopeOriginDefaultsToProximity", "internal/verify/scope_test.go:TestParseHunksArithmetic", "internal/cmd/verifyscope_test.go:TestPhaseScopeCollectsHunks"]
  notes = "An empty origin tag fails the test outright; in-hunk and inherited are distinguished and both are asserted to stay in the denominator. The classifier defaults to inherited, so a hunk-less degraded run cannot promote survivors to the strongest tier. All 19 of this run's in-scope survivors carry a tag (11 in-hunk, 8 inherited)."

[[finding]]
  severity = "NOTE"
  text = "accepted (unkillable — gremlins attribution ceiling): 9 in-hunk survivors on `switch { case ... }` conditions in internal/verify/scope.go:151, :153, :155 (the Source provenance switch) and :260 (the deleted-file-side case). All 9 are reported NOT COVERED, never LIVED — gremlins' go-cover attribution marks switch-case conditions uncovered even when exercised. TestNewScopeSourceProvenance asserts all three Source values plus the degraded reason, TestPhaseScopeSourceProvenance asserts both one-sided outcomes, and TestParseHunksIgnoresDeletedFileSide asserts the /dev/null case. Behaviourally covered; stop reporting."

[[finding]]
  severity = "NOTE"
  text = "accepted (equivalent mutant): 2 in-hunk survivors on internal/verify/scope.go:147, the `sort.Slice` comparator over a file's hunk ranges. Range order is not observable — Contains and InHunk scan every range — so a reversed or off-by-one comparator changes no behaviour, only the serialisation order in tests.json. Stop reporting."

[[finding]]
  severity = "NOTE"
  text = "deferred to survivor-lifecycle (r-02 routing): 8 inherited survivors on lines this phase did not change, in files it edited around — internal/mutation/gremlins.go:155 (x3) and :166 (error-message string concatenation on the unreadable-report and zero-coverage paths), internal/mutation/stryker.go:73, :80 (report-read and parse error branches) and :263 (the future-status default arm), internal/mutation/stryker_net.go:221 (the buildCmd process seam, stubbed out in unit tests). All are pre-existing adapter I/O and error plumbing, all reported NOT COVERED, and none participates in any criterion's covering test. Recorded here as accepted-for-now with a named destination; not to be re-reported by the next verify. (The spec's matching [[deferred]] entry says nine; the ground-truth count in tests.json is eight — same line list.)"

[[finding]]
  severity = "NOTE"
  text = "closed since the previous run: the two in-hunk survivors on internal/cmd/verify.go:472 (INVERT_NEGATIVES, ARITHMETIC_BASE) — printScopeSummary's `(+N more)` truncation branch — are killed. t-6 added TestVerifyCover_ScopeFileListTruncation and TestVerifyCover_ScopeFileListAtCapIsNotTruncated; the survivor set is otherwise identical to the partial run, so the delta is attributable to those two tests alone."

[[finding]]
  severity = "NOTE"
  text = "skipped README.md: no mutation adapter for .md"

[[finding]]
  severity = "NOTE"
  text = "skipped assets/prompts/verify.md: no mutation adapter for .md"

[[finding]]
  severity = "NOTE"
  text = "skipped internal/mutation/testdata/gremlins_pkg_report.json: no mutation adapter for .json"

[[finding]]
  severity = "NOTE"
  text = "skipped internal/mutation/testdata/strykernet_report.json: no mutation adapter for .json"

[[finding]]
  severity = "NOTE"
  text = "filtered 89 out-of-scope survivor(s) in files this phase did not touch — listed under `out_of_scope` in tests.json, not counted in the score"
