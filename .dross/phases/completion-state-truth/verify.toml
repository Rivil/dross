[verify]
  phase = "completion-state-truth"
  generated_at = 2026-08-02T13:56:24.254457Z
  verdict = "fail"
  finalized = true
  finalized_at = 2026-08-02T14:15:43.391027Z

[summary]
  mutation_status = "measured"
  mutation_score = 0.9582405345211581
  mutants_killed = 1721
  mutants_survived = 75
  mutants_not_covered = 75
  criteria_total = 6
  criteria_covered = 6
  criteria_uncovered = 0

[[criterion]]
  id = "c-1"
  status = "covered"
  tests = ["internal/cmd/ship_prompt_test.go:TestShipPromptNoUnguardedSwitch", "internal/cmd/ship_prompt_test.go:TestNoPromptPerformsRawBranchSwitch", "internal/cmd/ship_prompt_test.go:TestShipPromptNamesCompleteAsTeardownOwner", "internal/cmd/ship_test.go:TestShipWrongBranchRefusalNamesGuardedCheckout", "internal/cmd/phase_checkout_test.go:TestPhaseCheckoutRefusesClobber", "internal/cmd/phase_checkout_test.go:TestPhaseCheckoutSwitchesOnTheOrdinaryPath", "cmd/dross/main_test.go:TestShipPromptCommandsExist"]
  notes = "Three layers now: ship.md is scanned for --delete-branch, the GitLab/Forgejo remove-source-branch fields and raw checkout/switch; the guard was widened this run to walk EVERY prompt (t-11), asserting a non-zero scan count so a mis-rooted walk fails rather than reporting clean; and ship's own off-branch refusal is covered behaviourally (t-10) rather than by grep, since phase_checkout.go's doc comment legitimately spells the raw verb. The guarded replacement is proven to refuse a clobbering switch and to switch on the ordinary path. Both FLAGs the previous run raised against this criterion are closed."

[[criterion]]
  id = "c-2"
  status = "covered"
  tests = ["internal/cmd/phase_test.go:TestPhaseCompleteWritesCompletionRecord", "internal/cmd/phase_test.go:TestPhaseCompleteIsIdempotent", "internal/cmd/phase_test.go:TestCompleteOtherPhaseKeepsCurrent", "internal/cmd/completion_state_truth_test.go:TestShipToCompleteKeepsLiveState"]
  notes = "The write test pre-seeds no record, so deleting complete's write leaves zero entries and fails; the idempotence test asserts the breadcrumb count stays at exactly one over a re-run; the ship-side half asserts ship must NOT write it and leaves x/shipped."

[[criterion]]
  id = "c-3"
  status = "covered"
  tests = ["internal/cmd/squash_claim_guard_test.go:TestNoSurfaceClaimsCompletionRidesTheSquash", "internal/cmd/squash_claim_guard_test.go:TestCompleteHelpNamesRecordOwner", "internal/cmd/squash_claim_guard_test.go:TestQuickPromptHandlesShippedWindow", "internal/cmd/ship_test.go:TestShipNarratesCompleteOwnsRecord"]
  notes = "Whitespace-collapsed scan of an explicit file list (internal/cmd, internal/watch, prompts, changes.go, telemetry.go, ARCHITECTURE.md, README.md) over an 11-phrase family, plus a positive assertion that the help text names the owner and the location — so the surface cannot go silent instead of lying."

[[criterion]]
  id = "c-4"
  status = "covered"
  tests = ["internal/cmd/completion_state_truth_test.go:TestShipToCompleteKeepsLiveState", "internal/cmd/completion_state_truth_test.go:TestRawCheckoutStillClobbersLiveState"]
  notes = "Full ship -> simulated squash-merge on the bare origin -> complete, against a live state.json seeded with a long history at a distinct version, asserting entry count, first action and version all survive. The control proves the raw checkout DOES truncate that fixture, so the survival assertions are not vacuous, and that dross's guarded checkout refuses it."

[[criterion]]
  id = "c-5"
  status = "covered"
  tests = ["internal/cmd/phase_test.go:TestPhaseCompletePrintsTopologyStatement", "internal/cmd/phase_test.go:TestPhaseCompleteRemoteOnlyTeardown", "internal/cmd/phase_test.go:TestPhaseCompleteNamesRecordedBaseNotInferred", "internal/cmd/topology_test.go:TestBranchTopologyCountsAheadOfMain", "internal/cmd/topology_test.go:TestBranchTopologyWorkOverrideWins", "internal/cmd/topology_test.go:TestRenderTopologyLine", "internal/cmd/status_test.go:TestStatusTopologyLineAlways"]
  notes = "Upgraded from weak (previous run). Each clause of the completion statement is asserted separately, the recorded base beats an inferable milestone branch, the not-yet-on-main clause is pinned to AheadOfMain>0 in six render cases, and the status line is proven unconditional (mid-phase, between phases, no origin). The gap that forced the earlier downgrade — describeTeardown's fourth arm (origin still carries the branch, local ref already gone) was never executed, leaving two ARITHMETIC_BASE mutants alive at phase.go:625 — is closed by TestPhaseCompleteRemoteOnlyTeardown, which reaches that arm by recreating the origin ref from the retained tip SHA after the first complete. Both mutants are killed this run and its negative assertion excludes all three neighbouring arms, so collapsing this one into any of them fails rather than passing on a near-miss."

[[criterion]]
  id = "c-6"
  status = "covered"
  tests = ["internal/cmd/status_test.go:TestStatusReportsShippedNotStale", "internal/cmd/status_test.go:TestStatusShippedFromPRRecordAlone", "internal/cmd/status_test.go:TestStatusNoShippedWithoutEitherSignal", "internal/cmd/status_test.go:TestStatusSilentAfterCompletion", "internal/cmd/status_test.go:TestStatusNextStepAfterShip", "internal/watch/drift_test.go:TestDriftShippedPhaseNotUnshipped"]
  notes = "Positive (shipped: names branch, PR #42 and the base it waits on; phase line reads shipped) plus four false-positive controls (merged, squash-merged, on main, neither signal) and the post-completion silence case. The PR-record-alone case defeats Go's short-circuit so the tracked half is really executed. Drift carries the same rule with two controls."

[[finding]]
  severity = "BLOCKING"
  text = "t-12 added to this phase's scope during this verify run (user decision): internal/cmd/milestone.go:93 — `dross milestone prune`'s stale-HEAD refusal — still narrates \"switch to <main> first (`git checkout <main>`)\", the same unguarded verb t-10 removed from ship and t-11 removed from the prompts. It cannot reuse `dross phase checkout`, which is phase-branch-only (prefixes \"phase/\", refuses a missing local ref), so closing it needs a widened or sibling guarded verb. Unimplemented and untested: no criterion is uncovered by it (c-1 is scoped to /dross-ship steps and its own tests hold), but the phase cannot ship with agreed scope outstanding. Route: /dross-execute completion-state-truth --from t-12, then re-verify."

[[finding]]
  severity = "NOTE"
  text = "Zero surviving mutants inside this phase's diff. All 75 survivors are NOT-COVERED (never executed) rather than survived-despite-coverage, so efficacy over executed mutants is 1.00. Each was checked against the phase's diff hunks and falls outside them: phase.go 307/309/735/737, ship.go 246/520/522/524 (the previous run's 239/513/515/517, shifted +7 by t-10's edit), status.go 442/444/472/474, telemetry.go:46, and the rest. The new files topology.go and phase_checkout.go have zero survivors, and drift.go — also touched — has none."

[[finding]]
  severity = "NOTE"
  text = "The 75 pre-existing survivors are recorded as ROUTED, not re-listed as FLAGs (project/hard/r-02; user-confirmed this run). Their destinations are the two deferred entries already on this phase's spec.toml: promote r-02 to a builtin rule and give verify a survivor lifecycle (in-diff / routed / accepted-with-reason), and drain the standing pre-existing survivor set. The count fell 77 -> 75 this run because the only two in-diff survivors, phase.go:625, were killed by t-9."

[[finding]]
  severity = "NOTE"
  text = "Both FLAGs from the previous (partial) verify are closed and verified this run: c-5's phase.go:625 teardown-arm gap (t-9, mutants now killed) and ship.go:170's raw `git checkout` narration (t-10, now `dross phase checkout` with a behavioural test). Full suite green: go test ./... ok across all packages."

[[finding]]
  severity = "NOTE"
  text = "skipped ARCHITECTURE.md, README.md and assets/prompts/{execute,plan,quick,resume,ship,spec}.md: no mutation adapter for .md. Their behaviour is covered by the prompt-scanning guards under c-1 and c-3 instead."
