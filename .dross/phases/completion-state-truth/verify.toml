[verify]
  phase = "completion-state-truth"
  generated_at = 2026-08-02T15:23:39.945504Z
  verdict = "pass"
  finalized = true
  finalized_at = 2026-08-02T15:52:10.270097Z

[summary]
  mutation_status = "measured"
  mutation_score = 0.9583564686285397
  mutants_killed = 1726
  mutants_survived = 75
  mutants_not_covered = 75
  criteria_total = 6
  criteria_covered = 6
  criteria_uncovered = 0

[[criterion]]
  id = "c-1"
  status = "covered"
  tests = ["internal/cmd/ship_prompt_test.go:TestShipPromptNoUnguardedSwitch", "internal/cmd/ship_prompt_test.go:TestNoPromptPerformsRawBranchSwitch", "internal/cmd/ship_prompt_test.go:TestShipPromptNamesCompleteAsTeardownOwner", "internal/cmd/ship_test.go:TestShipWrongBranchRefusalNamesGuardedCheckout", "internal/cmd/phase_checkout_test.go:TestPhaseCheckoutRefusesClobber", "internal/cmd/phase_checkout_test.go:TestPhaseCheckoutSwitchesOnTheOrdinaryPath", "internal/cmd/phase_checkout_test.go:TestCheckoutRefusesClobberOnNonPhaseBranch", "internal/cmd/phase_checkout_test.go:TestCheckoutSwitchesOnTheOrdinaryPath", "internal/cmd/phase_checkout_test.go:TestCheckoutMissingBranch", "internal/cmd/milestone_test.go:TestPruneRefusalNamesGuardedCheckout", "cmd/dross/main_test.go:TestShipPromptCommandsExist", "cmd/dross/main_test.go:TestNarratedCommandsResolveAgainstTheTree", "cmd/dross/main_test.go:TestNarratedCommandsGuardCatchesBogusSubcommands"]
  notes = "The criterion's own surface — /dross-ship's steps — is covered three ways: ship.md is scanned for --delete-branch, the GitLab/Forgejo remove-source-branch fields and raw checkout/switch; the guard walks EVERY prompt with a non-zero scan-count assertion so a mis-rooted walk fails rather than reporting clean; and ship's off-branch refusal is covered behaviourally. The guarded surface extends past ship to `dross milestone prune`'s refusal via the top-level `dross checkout <branch>`, proven to refuse a clobbering switch, to switch on the ordinary path, and to refuse rather than create a missing ref. t-13 closes the reachability hole that made the previous run fail: the registration is now load-bearing. Re-established by hand-mutation THIS run — deleting `cmd.Checkout()` from cmd/dross/main.go turns TestNarratedCommandsResolveAgainstTheTree red with `milestone.go narrates `dross checkout` but no such command exists in the cobra tree` plus the same for phase_checkout.go, and the tree was restored clean afterwards. All four of t-13's test-contract items hold: the hand-mutation fails the guard and names both the command and the narrating files; `len(refs) == 0` fatals so a mis-rooted walk cannot pass vacuously; the bogus-subcommand sibling proves top-level resolution alone does not satisfy it (`dross phase nope` is rejected while `dross phase create` resolves); and narratedIgnore is empty, so its stale-entry and resolves-anyway assertions guard an empty set rather than masking anything."

[[criterion]]
  id = "c-2"
  status = "covered"
  tests = ["internal/cmd/phase_test.go:TestPhaseCompleteWritesCompletionRecord", "internal/cmd/phase_test.go:TestPhaseCompleteIsIdempotent", "internal/cmd/phase_test.go:TestCompleteOtherPhaseKeepsCurrent", "internal/cmd/completion_state_truth_test.go:TestShipToCompleteKeepsLiveState"]
  notes = "The write test pre-seeds no record, so deleting complete's write leaves zero entries and fails; the idempotence test asserts the breadcrumb count stays at exactly one over a re-run; the ship-side half asserts ship must NOT write it and leaves x/shipped. Unchanged since the previous run — the only source file to move since is cmd/dross/main_test.go — and re-confirmed green."

[[criterion]]
  id = "c-3"
  status = "covered"
  tests = ["internal/cmd/squash_claim_guard_test.go:TestNoSurfaceClaimsCompletionRidesTheSquash", "internal/cmd/squash_claim_guard_test.go:TestCompleteHelpNamesRecordOwner", "internal/cmd/squash_claim_guard_test.go:TestQuickPromptHandlesShippedWindow", "internal/cmd/ship_test.go:TestShipNarratesCompleteOwnsRecord"]
  notes = "Whitespace-collapsed scan of an explicit file list (internal/cmd, internal/watch, prompts, changes.go, telemetry.go, ARCHITECTURE.md, README.md) over an 11-phrase family, plus a positive assertion that the help text names the owner and the location — so the surface cannot go silent instead of lying. Unchanged this run."

[[criterion]]
  id = "c-4"
  status = "covered"
  tests = ["internal/cmd/completion_state_truth_test.go:TestShipToCompleteKeepsLiveState", "internal/cmd/completion_state_truth_test.go:TestRawCheckoutStillClobbersLiveState"]
  notes = "Full ship -> simulated squash-merge on the bare origin -> complete, against a live state.json seeded with a long history at a distinct version, asserting entry count, first action and version all survive. The control proves the raw checkout DOES truncate that fixture, so the survival assertions are not vacuous, and that dross's guarded checkout refuses it. Unchanged this run."

[[criterion]]
  id = "c-5"
  status = "covered"
  tests = ["internal/cmd/phase_test.go:TestPhaseCompletePrintsTopologyStatement", "internal/cmd/phase_test.go:TestPhaseCompleteRemoteOnlyTeardown", "internal/cmd/phase_test.go:TestPhaseCompleteNamesRecordedBaseNotInferred", "internal/cmd/topology_test.go:TestBranchTopologyCountsAheadOfMain", "internal/cmd/topology_test.go:TestBranchTopologyWorkOverrideWins", "internal/cmd/topology_test.go:TestRenderTopologyLine", "internal/cmd/status_test.go:TestStatusTopologyLineAlways"]
  notes = "Each clause of the completion statement is asserted separately, the recorded base beats an inferable milestone branch, the not-yet-on-main clause is pinned to AheadOfMain>0 in six render cases, and the status line is proven unconditional (mid-phase, between phases, no origin). The phase.go:625 teardown-arm gap that forced the earlier weak downgrade stays closed: both ARITHMETIC_BASE mutants are killed again this run, and topology.go carries zero survivors."

[[criterion]]
  id = "c-6"
  status = "covered"
  tests = ["internal/cmd/status_test.go:TestStatusReportsShippedNotStale", "internal/cmd/status_test.go:TestStatusShippedFromPRRecordAlone", "internal/cmd/status_test.go:TestStatusNoShippedWithoutEitherSignal", "internal/cmd/status_test.go:TestStatusSilentAfterCompletion", "internal/cmd/status_test.go:TestStatusNextStepAfterShip", "internal/watch/drift_test.go:TestDriftShippedPhaseNotUnshipped"]
  notes = "Positive (shipped: names branch, PR #42 and the base it waits on; phase line reads shipped) plus four false-positive controls (merged, squash-merged, on main, neither signal) and the post-completion silence case. The PR-record-alone case defeats Go's short-circuit so the tracked half is really executed. Drift carries the same rule with two controls; drift.go has zero survivors."

[[finding]]
  severity = "NOTE"
  text = "The BLOCKING finding from the previous run is closed and re-proven, not assumed. t-13 landed as 7bc81a2 (cmd/dross/main_test.go), so the phase is 13/13 with no agreed scope outstanding. The exact mutation that stayed green last run — deleting `cmd.Checkout()` from cmd/dross/main.go:36 — was re-run by hand this session and now fails TestNarratedCommandsResolveAgainstTheTree, naming both the unresolvable `dross checkout` and the two files that narrate it (milestone.go, phase_checkout.go). The working tree was restored to clean immediately after."

[[finding]]
  severity = "NOTE"
  text = "Zero surviving mutants inside this phase's diff, recomputed mechanically this run: every one of the 75 survivor line numbers was tested against the phase diff's changed-line ranges (merge-base 1d422dd9..HEAD, -U0 hunks over 22 changed .go files) and none falls inside a hunk. The files this phase created — topology.go, phase_checkout.go — have zero survivors, as do drift.go and milestone.go. All 75 survivors are NOT-COVERED (never executed) rather than survived-despite-coverage, so efficacy over executed mutants is 1.00. Counts are byte-identical to the previous run (1726 killed / 75 survived / 0.958): t-13 added a test file only, so it introduced no mutants."

[[finding]]
  severity = "NOTE"
  text = "The 75 pre-existing survivors are recorded as ROUTED, not re-listed as FLAGs, per project/hard/r-02 and the routing already on this phase's spec.toml. Their destinations are its deferred entries: promote r-02 to a builtin rule and give verify a survivor lifecycle (in-diff / routed / accepted-with-reason), and drain the standing pre-existing survivor set (doctor.go 11, hints.go 8, status.go 7, ship.go 6, env.go 5, phase_lifecycle.go 5, footer_coverage.go 4, gitignore.go 4, phase.go 4 and the rest). The count is unchanged at 75 across the last three runs — neither t-12 nor t-13 added any."

[[finding]]
  severity = "NOTE"
  text = "Residual scope boundary on t-13's guard, ROUTED this run to a third deferred entry on spec.toml (user decision, defer-not-add). TestNarratedCommandsResolveAgainstTheTree lifts `dross <verb> [<sub>]` only from backticked spans inside string literals, so an unbackticked narrated invocation escapes it — doctor.go:192 (\"    Fix: dross project set --unset board.state_map.%s\") and hooks.go:57-58 are live examples — as does a form split across a string concatenation. No criterion is affected: the narrations c-1 depends on (milestone.go:98, phase_checkout.go:88) are backticked and covered, and the backtick scope is exactly what t-13's task description and test contract specified. Recorded as NOTE rather than FLAG because it is an agreed boundary of the delivered contract about unrelated commands, not a shortfall against it."

[[finding]]
  severity = "NOTE"
  text = "gremlins skipped ./cmd/dross (zero covered mutants — coverage blind spot), which is the only package t-13 touched. This is why its guard had to be established by hand-mutation rather than by the mutation run, and why the mutation totals could not move this run."

[[finding]]
  severity = "NOTE"
  text = "Full suite observed green this run: go test ./... ok across all 29 packages (internal/cmd 86.412s, cmd/dross 0.330s), after the hand-mutation was reverted."

[[finding]]
  severity = "NOTE"
  text = "skipped ARCHITECTURE.md, README.md and assets/prompts/{execute,plan,quick,resume,ship,spec}.md: no mutation adapter for .md. Their behaviour is covered by the prompt-scanning guards under c-1 and c-3 instead."
