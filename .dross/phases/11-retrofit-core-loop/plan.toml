[phase]
id = "11-retrofit-core-loop"

[[task]]
id            = "t-1"
wave          = 1
title         = "Retrofit plan.md interaction"
files         = ["assets/prompts/plan.md"]
description   = """
Add `dross interaction show` to §0 pre-flight (MUST-FOLLOW) and a contract
reference in the intro. Uniform-restructure interactive turns: §3 propose as
single-decision propose-and-react, §2P.3 walk one turn per panel disagreement
(panel_disagreement_walk), §5 confirm with a one-line summary instead of pasting
plan.toml (c-4).
"""
covers        = ["c-1", "c-2", "c-3", "c-4"]
test_contract = [
  "if plan.md drops the `dross interaction show` pre-flight call, the core-loop wiring test (t-7) fails",
  "if plan.md loses its interaction-contract reference phrase, the wiring test (t-7) fails",
]
status        = "pending"

[[task]]
id            = "t-2"
wave          = 1
title         = "Retrofit execute.md interaction"
files         = ["assets/prompts/execute.md"]
description   = """
Add `dross interaction show` to §0 pre-flight (MUST-FOLLOW) and a contract
reference in the intro. Uniform-restructure §1c approach-approval and §1e
red-path turns into single-decision propose-and-react form (retrofit_depth).
"""
covers        = ["c-1", "c-2", "c-3", "c-4"]
test_contract = [
  "if execute.md drops the `dross interaction show` pre-flight call, the wiring test (t-7) fails",
  "if execute.md loses its interaction-contract reference phrase, the wiring test (t-7) fails",
]
status        = "pending"

[[task]]
id            = "t-3"
wave          = 1
title         = "Retrofit verify.md interaction"
files         = ["assets/prompts/verify.md"]
description   = """
Add `dross interaction show` to §0 pre-flight (MUST-FOLLOW) and a contract
reference in the intro. Restructure §4 surface to emit verdict + a compact
criterion→test/status mapping, not the raw verify.toml (verify_surface, c-4).
"""
covers        = ["c-1", "c-2", "c-3", "c-4"]
test_contract = [
  "if verify.md drops the `dross interaction show` pre-flight call, the wiring test (t-7) fails",
  "if verify.md loses its interaction-contract reference phrase, the wiring test (t-7) fails",
]
status        = "pending"

[[task]]
id            = "t-4"
wave          = 1
title         = "Retrofit ship.md interaction"
files         = ["assets/prompts/ship.md"]
description   = """
Add `dross interaction show` to §0 pre-flight (MUST-FOLLOW) and a contract
reference in the intro. Split §2 body-override and §3 reviewers into separate
single-decision turns (c-3). Keep the §1 PR-body preview shown in full as the
explicit outward-facing exception to c-4 (ship_body_preview).
"""
covers        = ["c-1", "c-2", "c-3", "c-4"]
test_contract = [
  "if ship.md drops the `dross interaction show` pre-flight call, the wiring test (t-7) fails",
  "if ship.md loses its interaction-contract reference phrase, the wiring test (t-7) fails",
]
status        = "pending"

[[task]]
id            = "t-5"
wave          = 1
title         = "Retrofit review.md interaction"
files         = ["assets/prompts/review.md"]
description   = """
Add `dross interaction show` to §0 pre-flight (MUST-FOLLOW) and a contract
reference in the intro. Restructure the §5 post-or-skip gate into a single-
decision propose-and-react turn (retrofit_depth).
"""
covers        = ["c-1", "c-2", "c-3", "c-4"]
test_contract = [
  "if review.md drops the `dross interaction show` pre-flight call, the wiring test (t-7) fails",
  "if review.md loses its interaction-contract reference phrase, the wiring test (t-7) fails",
]
status        = "pending"

[[task]]
id            = "t-6"
wave          = 2
depends_on    = ["t-1", "t-2", "t-3", "t-4", "t-5"]
title         = "Update audit doc core-loop sections"
files         = ["docs/interaction-audit.md"]
description   = """
Fill the decision-point rows + conformance for dross-plan/execute/verify/ship/
review reflecting the post-retrofit pattern, and reconcile the phase grouping so
verify/ship/review sit under phase 11 (not 13) to match the actual scope.
"""
covers        = ["c-5"]
test_contract = [
  "if any of the 5 core-loop sections loses its decision-point table, the existing audit enumeration test fails",
  "if a retrofitted command's conformance cell isn't marked conforming, the t-7 conformance assertion fails",
]
status        = "pending"

[[task]]
id            = "t-7"
wave          = 3
depends_on    = ["t-1", "t-2", "t-3", "t-4", "t-5", "t-6"]
title         = "Add core-loop wiring guard test"
files         = ["internal/cmd/interaction_coreloop_test.go"]
description   = """
New table-driven test over plan/execute/verify/ship/review: assert each prompt
invokes `dross interaction show`, carries no dead nested @-include line, and
contains the interaction-contract reference phrase. Also assert the 5 audit-doc
sections are marked conforming. This is the grep-verification for c-1/c-2/c-5.
"""
covers        = ["c-1", "c-2", "c-5"]
test_contract = [
  "if any of the 5 prompts lacks the emitter call or contract reference, this test fails",
  "if any core-loop audit section regresses to non-conforming, this test fails",
]
status        = "pending"
