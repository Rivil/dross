[verify]
  phase = "03-fix-completion-chore-divergence"
  generated_at = 2026-06-20T07:59:25.76244Z
  verdict = "pass"

[summary]
  mutation_status = "measured"
  mutation_score = 0.5788732394366197
  mutants_killed = 411
  mutants_survived = 298
  mutants_not_covered = 154
  criteria_total = 5
  criteria_covered = 5
  criteria_uncovered = 0

[[criterion]]
  id = "c-1"
  status = "covered"
  tests = [
    "internal/cmd/phase_test.go:TestPhaseCompleteHappyPath",
    "internal/cmd/phase_test.go:TestShipToCompleteLeavesZeroManualGit",
  ]
  notes = "complete runs ff + branch-delete with no manual git; ff (phase.go:249) and branch-delete guards (255,271) are mutation-clean."

[[criterion]]
  id = "c-2"
  status = "covered"
  tests = [
    "internal/cmd/phase_test.go:TestConsecutivePhasesNoDivergence",
    "internal/cmd/phase_test.go:TestShipToCompleteLeavesZeroManualGit",
  ]
  notes = "Explicit localMain == originMain assertion after each complete (phase_test.go:530)."

[[criterion]]
  id = "c-3"
  status = "covered"
  tests = ["internal/cmd/phase_test.go:TestConsecutivePhasesNoDivergence"]
  notes = "Two back-to-back ship→squash→complete cycles; the 2nd phase ff would break if cycle 1 re-seeded divergence."

[[criterion]]
  id = "c-4"
  status = "covered"
  tests = [
    "internal/cmd/ship_test.go:TestShipFullFlowAgainstMockProvider",
    "internal/cmd/ship_test.go:TestShipReshipIsIdempotent",
  ]
  notes = "Asserts the PUSHED ref carries cleared current_phase + `completed <id>` and HEAD is `chore(dross): ship <id>` (committed pre-push). Fold write+commit incl. idempotency gate (ship.go:155) is mutation-clean."

[[criterion]]
  id = "c-5"
  status = "covered"
  tests = [
    "internal/cmd/phase_test.go:TestPhaseCompleteRefusesUnmergedUpstream",
    "internal/cmd/phase_test.go:TestPhaseCompleteRefusesUnmergedNoLocalBranch",
  ]
  notes = "Branch-ref-independent guard (phase.go:240-247): refuses + mutates nothing when origin lacks the completion record, even with the local branch deleted. Guard conditionals (241,244) are mutation-clean."

# --- Findings -------------------------------------------------------------
# NOTE: the dross verify skeleton auto-seeded ~290 FLAG findings, one per
# surviving mutant across the WHOLE internal/cmd package. That list was
# replaced with the phase-relevant subset below — see the scope NOTE.

[[finding]]
  severity = "NOTE"
  text = "Mutation was MEASURED but PACKAGE-SCOPED: gremlins mutated all of internal/cmd, not just the phase-touched ship.go/phase.go. The 0.58 package score is dominated by unrelated commands (stats.go, doctor.go, issue.go, status.go, rule.go, env.go, …) and is NOT a phase-level signal — the <0.60 cutoff does not apply to this verdict (documented FeastAhead-class scope mismatch). Every criterion-bearing line in the touched files (fold ship.go:155, merge guard phase.go:241/244, ff :249, branch-delete guards :255/271) had its mutants KILLED."

[[finding]]
  severity = "FLAG"
  text = "Tooling: the gremlins adapter mutates per-package, not phase-file-scoped, so every phase's verify mutation score is diluted by unrelated commands in the same package. Worth narrowing the adapter to the phase's changed files."

[[finding]]
  severity = "FLAG"
  text = "Surviving mutants in touched files but in NON-criterion code (no criterion depends on them): ship.go shipComment (259-291) + shipResultTag (303-307) — pre-existing PR-comment/telemetry code untouched by phase 03; phase-id resolution switch phase.go:184/186 and mainBranch=='' default :200 — both resolution paths reach the same id in tests; nextPhaseNumber phase.go:392/399 — the separate phase-create numbering found-bug (tracked as an open loop, not a phase-03 criterion)."
