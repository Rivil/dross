[verify]
  phase = "tool-output-not-persisted"
  generated_at = 2026-09-09T18:01:10.512336Z
  verdict = "fail"
  finalized = true
  finalized_at = 2026-09-09T18:05:12.717573Z

[summary]
  mutation_status = "measured"
  measured_on = "helicon"
  mutation_score = 0.8697318007662835
  mutants_killed = 227
  mutants_survived = 34
  mutants_not_covered = 34
  mutants_in_scope = 261
  unclassified_in_scope = 2
  criteria_total = 6
  criteria_covered = 5
  criteria_uncovered = 1

  [[summary.leg]]
    language = "go"
    tool = "gremlins"
    killed = 227
    survived = 34
    timeout = 0
    in_scope = 261
    score = 0.8697318007662835
    file_count = 23
    measured_on = "helicon"

[[criterion]]
  id = "c-1"
  status = "weak"
  tests = ["internal/mutation/toolfail_test.go:TestRecordErrorClauses", "internal/mutation/toolfail_test.go:TestRecordToolFailureShape", "internal/mutation/toolfail_test.go:TestRecordStartFailure", "internal/mutation/toolfail_test.go:TestNotCapturedCarriesNoByteCount", "internal/mutation/gremlins_failure_test.go:TestGremlinsReportlessExitIsRecorded", "internal/mutation/gremlins_failure_test.go:TestGremlinsDetachedCollectRecordsNotCaptured", "internal/mutation/stryker_net_failure_test.go:TestStrykerNetNoReportIsRecordedAtTheCallSite", "internal/mutation/toolfence_residual_test.go:TestNoToolOutputInsideAnErrorConstructor"]
  notes = "The record and its four facts are pinned, and gremlins (3 sites) plus stryker.net (2) assert errors.As(err, &toolFailure). Downgraded on two in-hunk survivors in the recorder itself. toolfail.go:25: TestRecordStartFailure passes exitDidNotStart as the argument it is asserting on, so flipping the const to 1 satisfies the equality and the test still reads 'did not start' - no test in package mutation asserts a real exit status of 1. toolfail.go:139: RecordLegError has no package-local caller or test at all; its nil-guard inversion is killed only by internal/verify's TestCarrierDoesNotSwallowDrossAuthoredDiagnostics, which gremlins' per-package model cannot see. Separately, stryker's own failure path asserts prose, not the type: reproducing the record's wording inline would pass it while breaking 'one shared implementation'."

[[criterion]]
  id = "c-2"
  status = "covered"
  tests = ["internal/verify/persist_toolfence_test.go:TestFailedLegReachesDiskClean", "internal/verify/persist_toolfence_test.go:TestPersistenceAssertionsCanFire", "internal/mutation/stryker_failure_test.go:TestStrykerReportlessSplitsTerminalFromError"]
  notes = "Both artifacts are read back off disk and asserted for the exit status, the observed byte count and the stderr pointer, and against the canary and the head banner. Verified by breakage: re-embedding the head quote into stryker.go's error fails TestStrykerReportlessSplitsTerminalFromError. TestPersistenceAssertionsCanFire drives the pre-phase leaky error through the same path and asserts the canary DOES land, so the absence checks are shown capable of failing. Seam recorded: strykerBuildCmd is unexported, so the disk-side test builds the leg error through the exported recorder in the shape Stryker.Run returns rather than driving the real adapter; the two halves meet on a duplicated literal at stryker.go:154 and persist_toolfence_test.go:27."

[[criterion]]
  id = "c-3"
  status = "covered"
  tests = ["internal/mutation/stryker_test.go:TestStrykerHeadBufferIsBoundedAndDoesNotSwallowTheStream", "internal/mutation/toolfail_test.go:TestPrintHeadMatchesTheQuoteItReplaced", "internal/mutation/stryker_failure_test.go:TestStrykerReportlessSplitsTerminalFromError", "internal/mutation/stryker_test.go:TestStrykerDropWarningReachesTheTerminal"]
  notes = "The tee still delivers the full 2.4MB stream to os.Stderr, and printHead is pinned byte-for-byte against the quote() it replaced, including the truncation tail and the silent case. All three adapters assert the cause appears on stderr twice - teed live, then re-printed under the head banner at the failure point. Verified by breakage: deleting the re-print fails both the stderr-count assertion and the retargeted line cap."

[[criterion]]
  id = "c-4"
  status = "covered"
  tests = ["internal/cmd/toolfence_enum_test.go:TestEveryPersistedTextSinkIsDeclared", "internal/cmd/toolfence_carrier_test.go:TestEveryRecordedWriteComesFromTheCarrier", "internal/cmd/toolfence_carrier_test.go:TestCarrierScanIsCalibrated", "internal/cmd/toolfence_enum_test.go:TestNoStaleDeclaration", "internal/cmd/toolfence_enum_test.go:TestVocabularyIsExact", "internal/cmd/toolfence_carrier_test.go:TestRecordedFieldsCannotBeReclassifiedAway", "internal/toolfence/fields_test.go:TestValidateNamesEveryMalformedShape"]
  notes = "Both arms are calibrated on fixtures rather than on the tree's post-fix state, and the reclassification escape is closed from both sides. Verified by breakage: reverting verify.go:571 to err.Error() reports it by file and line. Reconciliation noted - the criterion says 'every assignment to a declared field comes from the shared recorder', but 8 of the 10 declared fields are NotToolStream or Renderable and are assigned dross-authored prose by design; the disposition is what 'declared' means here, and the carrier arm binds the 2 Recorded ones."

[[criterion]]
  id = "c-5"
  status = "covered"
  tests = ["internal/cmd/toolfence_composer_test.go:TestNoComposerRendersARecordedField", "internal/cmd/toolfence_composer_test.go:TestNoBodySinkReadsARecordedField", "internal/cmd/toolfence_composer_test.go:TestBodyScanIsKeyedOnTheSinkNotTheFuncShape", "internal/cmd/toolfence_composer_test.go:TestShipCommentIsAPassThrough"]
  notes = "Both halves run: the four named composers are called with a canary in every Recorded field, and a sink-keyed AST scan resolves the body sites a func-shaped scan cannot see, with a per-group vacuity floor so an empty PR group fails. ship.go:559 is the only PR-comment site in the tree and its body comes only from --body/--body-file, asserted rather than declared. Verified by breakage: making BuildPRBody render LegSummary.Error fails the canary half."

[[criterion]]
  id = "c-6"
  status = "covered"
  tests = ["internal/cmd/toolfence_composer_test.go:TestNoBodySinkReadsARecordedField", "internal/cmd/toolfence_composer_test.go:TestBodyScanTripsOnTheLeakyFixture", "internal/cmd/toolfence_composer_test.go:TestNoComposerRendersARecordedField"]
  notes = "The same enumerating test over the board group: 9 sink sites across issue.go and issue_task.go, including the two inline backlog-item bodies a func-shaped scan resolves as zero. leaky_body.go.txt pins the trip, the Renderable pass, and the err.Error() exclusion on a written-down answer."

[[finding]]
  severity = "BLOCKING"
  text = "gremlins mutant survived: internal/mutation/toolfail.go:25 (INVERT_NEGATIVES)"

[[finding]]
  severity = "BLOCKING"
  text = "gremlins mutant survived: internal/mutation/toolfail.go:139 (CONDITIONALS_NEGATION)"

[[finding]]
  severity = "FLAG"
  text = "t-6's residual guard (TestNoToolOutputInsideAnErrorConstructor) misses the laundered-variable shape, which is the exact pre-phase shape: rendering into a strings.Builder on one line and passing quoted.String() to fmt.Errorf on the next is not reported. Demonstrated by re-introducing stryker.go's pre-phase branch verbatim - the behavioural canary tests caught it, the residual guard did not. The blind spot is documented in the file, but as dataflow-in-general rather than as 'the shape this phase just removed'."

[[finding]]
  severity = "FLAG"
  text = "stryker's failure path has no errors.As(err, &toolFailure) pin. gremlins (3 sites) and stryker.net (2) all assert the record structurally; stryker asserts only its prose (exit status, observed, stderr). Reproducing the record's wording inline would pass stryker_failure_test.go while breaking c-1's 'one shared implementation'. One assertion closes the asymmetry."

[[finding]]
  severity = "NOTE"
  text = "stryker.go:138-145 still carries the pre-phase comment block ending 'Quote it.' directly above the code that deliberately no longer quotes. As written the comment now states the opposite of what the branch does."

[[finding]]
  severity = "NOTE"
  text = "32 of the 34 in-scope survivors were accepted this run under two categories in .dross/survivors.toml: gremlins-switch-case (6, Validate's switch case-conditions, each arm exercised by TestValidateNamesEveryMalformedShape) and gremlins-var-initializer (26, string-literal concatenation inside the package-level `var fields` registry). Both are go-cover attribution ceilings, not test gaps. unclassified_in_scope was reduced from 34 to 2 by that drain without re-running the mutation leg - the classification is a pure function of tests.json and survivors.toml, but dross offers no re-derive, so the alternative was a second 1h40m run that could not change the verdict."

[[finding]]
  severity = "NOTE"
  text = "mutation efficacy over the 227 reachable mutants is 1.00; the 0.87 headline is 34 mutants gremlins attributes no coverage block to. Read the pair, not the headline."

[[finding]]
  severity = "NOTE"
  text = "`dross doctor` reports ~90 architecture-link drifts, including this phase's own (LanguageRun.Error, Stryker.Run, strykerPin). Advisory; clear with `dross architecture check --fix` before or during ship."
