[verify]
  phase = "10-interaction-contract"
  generated_at = 2026-06-21T15:26:56.435213Z
  verdict = "partial"

[summary]
  mutation_status = "measured"
  mutation_score = 0.8086185044359949
  mutants_killed = 638
  mutants_survived = 0
  mutants_not_covered = 151
  criteria_total = 4
  criteria_covered = 3
  criteria_uncovered = 1

[[criterion]]
  id = "c-1"
  status = "covered"
  tests = [
    "internal/rules/rules_test.go:TestRenderEmitsInteractionContractBuiltin",
    "internal/rules/rules_test.go:TestInteractionRuleNamesSnippet",
    "internal/rules/rules_test.go:TestInteractionRuleStaysTerse",
  ]
  notes = "Rule renders into dross rule show at hard severity with the canonical phrases and names the snippet; gremlins killed every covered mutant in rules.go's render path."

[[criterion]]
  id = "c-2"
  status = "covered"
  tests = [
    "internal/cmd/interaction_snippet_test.go:TestInteractionSnippetHasPlaybookMarkers",
    "internal/cmd/interaction_snippet_test.go:TestInteractionSnippetHasAcceptRewordDropExample",
    "internal/cmd/install_symlink_test.go:TestMakeInstallLinksInteractionSnippet",
    "internal/cmd/install_symlink_test.go:TestMakeDoctorCountsInteractionSnippet",
    "internal/rules/rules_test.go:TestInteractionContractNoDrift",
  ]
  notes = "Snippet content (four playbook markers + accept/reword/drop example), make-install symlink resolution, and rule/snippet drift are all asserted."

[[criterion]]
  id = "c-3"
  status = "weak"
  tests = [
    "internal/cmd/interaction_pilot_test.go:TestSpecPilotIncludesSnippet",
  ]
  notes = "Mechanical half proven: spec.md carries the literal @-include line and it resolves against the installed symlink. The criterion's core claim — the snippet text actually REACHES the model through the two-level nested include — is the irreducibly manual pilot, still PENDING (t-6 in_progress). Recorded in docs/interaction-audit.md with the sentinel to check and the CLI-emitter fallback. Weak, not covered, until the manual check passes."

[[criterion]]
  id = "c-4"
  status = "covered"
  tests = [
    "internal/cmd/interaction_audit_test.go:TestInteractionAuditEnumeratesEveryInteractiveCommand",
  ]
  notes = "Every interactive command (grep -l AskUserQuestion assets/commands/) has a per-decision-point section with a conformance column; a new interactive command without a section fails the enumeration."

[[finding]]
  severity = "FLAG"
  text = "c-3 is weak: the @-include pilot is proven mechanically but the end-to-end 'text reaches the model' check is a pending manual step (t-6 in_progress). Run /dross-spec in a fresh session and confirm the sentinel in docs/interaction-audit.md, then mark t-6 done and re-verify for pass."

[[finding]]
  severity = "NOTE"
  text = "Mutation ran module-wide via gremlins: killed=638, score=0.81. The 151 'survived' the skeleton seeded are all NOT-COVERED mutants (Survived==NotCovered==151) — zero true survivors; efficacy on covered mutants = 1.00. They sit in pre-existing untested paths across the cmd package (ship.go, issue.go, stats.go, etc.), none in this phase's criterion-covering tests, so no criterion is downgraded. The 0.81 score is depressed by module-wide not-covered code, not by weak phase tests. (Per the known gremlins not-covered conflation — see dross_gremlins_adapter note.)"

[[finding]]
  severity = "NOTE"
  text = "skipped assets/prompts/_interaction.md, assets/prompts/spec.md, docs/interaction-audit.md: no mutation adapter for .md (content guarded by Go tests reading the files instead)."
