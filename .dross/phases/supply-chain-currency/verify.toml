[verify]
  phase = "supply-chain-currency"
  generated_at = 2026-09-14T09:56:17.94986Z
  verdict = "pass"
  finalized = true
  finalized_at = 2026-09-14T09:57:12.535256Z

[summary]
  mutation_status = "skipped"
  measured_on = "local"
  mutation_score = 0.0
  mutants_killed = 0
  mutants_survived = 0
  mutants_not_covered = 0
  mutants_in_scope = 0
  unclassified_in_scope = 0
  criteria_total = 5
  criteria_covered = 5
  criteria_uncovered = 0

[[criterion]]
  id = "c-1"
  status = "covered"
  tests = [".github/workflows/ci.yml:72 govulncheck ./... (CI gate — fails on any reachable advisory)", "internal/cmd/toolchain_source_test.go:28 TestToolchainSingleSource"]
  notes = "Observed: govulncheck@v1.8.0 on go1.27.1 → 0 vulnerabilities affecting dross code (1 module-level, unreachable: GO-2026-5932 x/crypto/openpgp, no fix). CI-log `go version` evidence lands on first push."

[[criterion]]
  id = "c-2"
  status = "covered"
  tests = ["internal/cmd/toolchain_source_test.go:28 TestToolchainSingleSource", "internal/cmd/toolchain_source_test.go:141 TestSetupGoStepScanner"]
  notes = "Observed failing on both mutations: toolchain line removed from go.mod; go-version: key re-added to a setup-go step."

[[criterion]]
  id = "c-3"
  status = "covered"
  tests = [".github/workflows/ci.yml:54 go mod tidy -diff (CI gate — non-zero on drift)"]
  notes = "Observed: `go mod tidy -diff` exit 0 on the committed tree; `go list -m -u all` shows no bracketed update on any direct dep (toml 1.6.0, cobra 1.10.2, pflag 1.0.10, x/mod 0.41.0, x/term 0.46.0, minisign 0.3.0). Latest-ness is point-in-time evidence by spec design."

[[criterion]]
  id = "c-4"
  status = "covered"
  tests = ["internal/cmd/release_pipeline_test.go:20 TestReleaseJobBuildsReadonly", "internal/cmd/release_pipeline_test.go:31 TestGoreleaserHasNoBeforeHooks", "internal/cmd/release_pipeline_test.go:47 TestReleaseJobRunsGovulncheck", "internal/cmd/release_pipeline_test.go:92 TestGovulncheckPinAgrees"]
  notes = "Each observed failing on its reverting mutation (GOFLAGS removed; before: hook re-added; `|| true` appended; ci.yml pin drifted to v1.6.0)."

[[criterion]]
  id = "c-5"
  status = "covered"
  tests = ["internal/cmd/workflow_run_expressions_test.go:148 TestWorkflowsHaveNoExpressionsInRun", "internal/cmd/workflow_run_expressions_test.go:85 TestRunBlockScanner"]
  notes = "Sweep observed failing on a re-inlined `${{ github.event.inputs.version }}`; scanner fixtures cover single-line, literal/folded blocks, block termination, and env:/with:/if: false positives."

[[finding]]
  severity = "NOTE"
  text = "skipped .github/workflows/ci.yml: no mutation adapter for .yml"

[[finding]]
  severity = "NOTE"
  text = "skipped .github/workflows/release.yml: no mutation adapter for .yml"

[[finding]]
  severity = "NOTE"
  text = "skipped .goreleaser.yaml: no mutation adapter for .yaml"

[[finding]]
  severity = "NOTE"
  text = "skipped go.mod: no mutation adapter for .mod"

[[finding]]
  severity = "NOTE"
  text = "skipped go.sum: no mutation adapter for .sum"

[[finding]]
  severity = "NOTE"
  text = "skipped internal/cmd/release_pipeline_test.go: no mutation adapter for .go"

[[finding]]
  severity = "NOTE"
  text = "skipped internal/cmd/toolchain_source_test.go: no mutation adapter for .go"

[[finding]]
  severity = "NOTE"
  text = "skipped internal/cmd/workflow_run_expressions_test.go: no mutation adapter for .go"

[[finding]]
  severity = "NOTE"
  text = "skipped internal/mutation/ceiling_test.go: no mutation adapter for .go"

[[finding]]
  severity = "NOTE"
  text = "mutation skipped: --skip-mutation passed (user decision) — the phase changed no non-test Go (four _test.go files, go.mod/go.sum, workflow YAML), so gremlins would have measured 0 in-scope mutants; verdict rests on criterion coverage alone"

[[finding]]
  severity = "NOTE"
  text = "ci.yml's govulncheck and tidy -diff gates (c-1, c-3) are exercised on every PR but their presence is not pinned by a Go test the way release.yml's govulncheck step is (TestReleaseJobRunsGovulncheck); a one-line content guard would close that asymmetry"

[[finding]]
  severity = "NOTE"
  text = "helicon's distro Go had GOTOOLCHAIN=local compiled in; `go env -w GOTOOLCHAIN=auto` was set there during execute so the remote gate and gremlins leg resolve go1.27.1 from go.mod — host config, not repo state"
