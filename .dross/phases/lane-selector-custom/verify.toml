[verify]
  phase = "lane-selector-custom"
  generated_at = 2026-08-30T17:38:58.937283Z
  verdict = "fail"
  finalized = true
  finalized_at = 2026-08-30T19:17:40.259559Z

[summary]
  mutation_status = "measured"
  measured_on = "helicon"
  mutation_score = 0.971830985915493
  mutants_killed = 414
  mutants_survived = 12
  mutants_not_covered = 12
  mutants_in_scope = 426
  unclassified_in_scope = 1
  criteria_total = 4
  criteria_covered = 4
  criteria_uncovered = 0

  [[summary.leg]]
    language = "go"
    tool = "gremlins"
    killed = 414
    survived = 12
    timeout = 0
    in_scope = 426
    score = 0.971830985915493
    file_count = 14

[[criterion]]
  id = "c-1"
  status = "covered"
  tests = ["internal/cmd/validate_lane_test.go:563 TestValidateRejectsSelectorTemplateWithNoSelector", "internal/cmd/validate_lane_test.go:587 TestValidateRejectsSelectorTemplateWithNoPlaceholder", "internal/cmd/validate_lane_test.go:612 TestValidateRejectsSelectorJoinWithNoPathsPlaceholder", "internal/cmd/validate_lane_test.go:652 TestValidateAcceptsAFullyPopulatedTemplateLane", "internal/cmd/test_files_test.go:377 TestTemplatedLaneSpawnsTheExpandedLine", "internal/cmd/test_files_test.go:400 TestTemplatedLaneJoinsPathsIntoOneArgument", "internal/cmd/test_files_test.go:451 TestMalformedTemplateRefusesBeforeAnyLaneSpawns", "internal/testlane/template_test.go:16 TestExpandRepeatsTheTemplatePerPath"]
  notes = "Both halves are asserted. Rejection: each malformed shape (no selector, no placeholder, join with no {paths}) fails validate AND the assertion requires the lane name in the output, so a refusal that did not name the offending lane fails. The accept case is the anti-over-refusal assertion a blanket 'refuse all templates' rule would fail alone, and its ctest lane spells `a{2,3}` so a regex quantifier is not mistaken for an unknown placeholder. Scoped run: asserted on the RECORDED spawn line (`cargo test --package 'crates/a/src' --package 'crates/b/src'`), so a lane that expanded a template and then spawned its bare command fails."

[[criterion]]
  id = "c-2"
  status = "covered"
  tests = ["internal/cmd/test_lane_consent_test.go:796 TestAddingASelectorTemplateStalesTheGrant", "internal/cmd/test_lane_consent_test.go:815 TestChangingSelectorJoinAloneStalesTheGrant", "internal/cmd/test_lane_consent_test.go:843 TestLaneWithNoTemplateKeepsItsPrePhaseConsentLine", "internal/cmd/test_lane_consent_test.go:872 TestTemplateAndPrepareNamespacesAreDisjoint", "internal/cmd/test_lane_consent_test.go:936 TestAllThreeBoundFieldsFingerprintDistinctly", "internal/cmd/test_files_test.go:486 TestTemplatedPathCarryingAMetacharacterIsQuoted", "internal/testlane/template_test.go:63 TestExpandQuotesAPathCarryingShellMetacharacters", "internal/testlane/template_test.go:89 TestExpandKeepsASubstitutedPathOptionSafe"]
  notes = "Staleness: adding a template, changing the join, and dropping the join each move the fingerprint; the grant-preservation half asserts against LITERAL pre-phase strings rather than laneConsentLine's own output, which would pass for any self-consistent rewrite. The disjointness test compares >100 grantable lanes with both frame prefixes appearing as ordinary field CONTENT, so a spoofable framing collides there. One-argument: asserted at both layers — the expander quotes, and the run site asserts `cargo test --only 'crates/a; touch pwned'` reaches sh -c as a single argument, so an unquoted path that became a second command fails."

[[criterion]]
  id = "c-3"
  status = "covered"
  tests = ["internal/cmd/test_lane_edit_test.go:388 TestLaneEditRewritesEveryInPlaceFieldKeepingPosition", "internal/cmd/test_lane_edit_test.go:352 TestLaneEditRewritesTheCommandInPlaceAndStalesTheGrant", "internal/cmd/test_lane_edit_test.go:450 TestLaneEditRefusesAMalformedLaneBeforeTheWrite", "internal/cmd/test_lane_edit_test.go:489 TestLaneEditReadsTheProposedLaneAlone", "internal/cmd/test_lane_edit_test.go:510 TestLaneEditClearGesturesDropTheirFields", "internal/cmd/test_lane_edit_test.go:541 TestLaneEditOmittedFlagsLeaveEveryOtherFieldAlone"]
  notes = "The widened field set is walked case by case (match, selector, selector normalization, empty_exit, selector_template, selector_join); each case also asserts the lane kept its POSITION and the neighbour is byte-identical, which is what a remove-then-append implementation fails while still getting the value right. Stale-not-absent is asserted directly (ConsentStale, plus the `dross trust --lane go` fix line), and that is the distinction remove-then-re-add got wrong. The refusal table compares project.toml as BYTES, so a refusal that round-tripped the same content through load-mutate-save still fails."

[[criterion]]
  id = "c-4"
  status = "covered"
  tests = ["internal/cmd/validate_lane_test.go:715 TestValidateWarnsOnAScopedWholeTreeCommand", "internal/cmd/validate_lane_test.go:751 TestTheWholeTreeWarningDoesNotFailValidate", "internal/cmd/validate_lane_test.go:771 TestNoWarningForAScopedCommand", "internal/cmd/validate_lane_test.go:801 TestAWarningAlongsideAProblemStillFails", "internal/cmd/test_lane_test.go:537 TestLaneAddWarnsOnAScopedWholeTreeCommandAndStillWrites", "internal/cmd/test_lane_edit_test.go:672 TestLaneEditWarnsOnAScopedWholeTreeCommand", "internal/cmd/test_lane_edit_test.go:701 TestLaneEditDoesNotWarnWhenTheCommandStopsBeingWholeTree"]
  notes = "All three declared surfaces are covered (validate, lane add, lane edit) for both tokens (./... and .), and each asserts the lane AND the token are named. The exit contract is stated on its own and is non-vacuous — it asserts a warning was actually printed before asserting it did not fail the gate. The false-positive half is covered at every surface, including a lane that does not scope at all and one whose command is ./cmd/... rather than ./..., and TestAWarningAlongsideAProblemStillFails proves the warning tier does not swallow a real fault beside it."

[[finding]]
  severity = "BLOCKING"
  text = "gremlins mutant survived: internal/cmd/test_lane.go:69 (CONDITIONALS_NEGATION) — laneSelectorRefusal is DEAD CODE this phase orphaned, not an attribution artefact. It has zero callers: t-5/t-6 replaced the per-field-group gate with laneRefusal, which runs laneProblems over a synthetic one-lane project, and laneProblems already calls laneSelectorProblems (internal/cmd/validate.go:262) — so the dead function's check is a strict subset of the live gate. Its own doc comment at test_lane.go:135 already describes it in the past tense. Fix: delete the function and its doc comment, and trim the stale reference in laneRefusal's comment. Disposition decided by the user 2026-08-30: fix in this phase rather than route or accept."

[[finding]]
  severity = "NOTE"
  text = "laneToolchainRefusal (internal/cmd/test_lane.go:82) is redundant in the same way — laneProblems calls laneToolchainProblems — but it still has two live callers (test_lane.go:249, :538), so it is duplicated work rather than dead code. Not this phase's survivor; noted for whoever consolidates the CLI gate."

[[finding]]
  severity = "NOTE"
  text = "11 of the 12 in-scope survivors are the accepted internal/cmd/validate.go attribution ceiling (gremlins reports NOT COVERED for lines the suite provably executes; proven by TestAttributionCeilingIsReal). Efficacy over the 414 reachable mutants is 1.00."

[[finding]]
  severity = "NOTE"
  text = "skipped README.md: no mutation adapter for .md"

[[finding]]
  severity = "NOTE"
  text = "skipped assets/prompts/options.md: no mutation adapter for .md"
