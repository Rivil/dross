name: ci

on:
  push:
    branches: [main]
  pull_request:

# Least privilege: this workflow only reads the repo. No job pushes anywhere.
permissions:
  contents: read

# Supersede, don't queue: a newer push to a PR cancels the run it replaces, so
# a dross ship's back-to-back pushes cost one pipeline, not three. Only PR runs
# share a group. The run_id arm gives every main push a group of its own —
# main auto-releases, so each commit keeps its own verdict, and GitHub replaces
# a pending run in a shared group even without cancel-in-progress.
# TestCIConcurrencyLive pins this block.
concurrency:
  group: ci-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      # Supply-chain hardening: pin the module proxy + checksum DB and refuse any
      # mid-CI mutation of go.mod/go.sum.
      GOFLAGS: -mod=readonly
      GOPROXY: https://proxy.golang.org,direct
      GOSUMDB: sum.golang.org
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          # Full history: TestRedProofPinsBaseCommit resolves the base commit
          # fixtures/hostile-config-c5/RUN.md pins, which a depth-1 checkout
          # does not carry. Without this the check degrades to a log line.
          fetch-depth: 0

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          # The toolchain is pinned in exactly one place: go.mod's `toolchain`
          # directive. setup-go v7 reads that line (v5 read only the `go`
          # directive, so the real toolchain arrived later via GOTOOLCHAIN=auto
          # and the declared version was a fiction). A hand-copied patch string
          # here would be a second source that drifts — the govulncheck gate
          # below scans whatever toolchain actually runs, so the one go.mod
          # names must be the one installed. TestToolchainSingleSource fails
          # if this step ever grows a `go-version:` key again. Bump the
          # toolchain in go.mod when a stdlib advisory lands.
          go-version-file: go.mod
          cache: true

      # Log the toolchain that the steps below — govulncheck included — ran on,
      # so the CI log itself proves go.mod's pin is the version scanned.
      - name: go version
        run: go version

      # Tidy drift gate: the committed go.mod/go.sum must already be the tidy
      # graph. -diff exits non-zero and prints the delta instead of rewriting
      # (which -mod=readonly would refuse anyway). Release builds readonly with
      # no tidy hook, so drift caught here is drift that would otherwise fail —
      # or silently reshape — the release build.
      - name: go mod tidy -diff
        run: go mod tidy -diff

      - name: go vet
        run: go vet ./...

      # -timeout 20m, not go test's default 10m: internal/cmd under -race grew
      # from ~200s to 525-600s+ in a week and began tripping the default on
      # identical code (PR #131). Headroom only — finding what grew is phase
      # cmd-test-duration, and this ceiling drops back once it lands.
      - name: go test
        run: go test -race -count=1 -timeout 20m ./...

      - name: go build
        run: go build ./cmd/dross

      # Supply-chain gate: pinned, never @latest — a compromised release must
      # not be able to walk in on the next CI run. No allowlist and no
      # `|| true`: a finding fails the build and the dep gets upgraded.
      - name: install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@v1.8.0

      - name: govulncheck
        run: govulncheck ./...

  # The non-Go mutation leg. Until this job existed, dross's TypeScript adapter
  # was only ever exercised where a developer happened to have Node installed —
  # everywhere else TestStrykerRunsEndToEnd skipped, and a skipped test reports
  # the same green as one that ran. So the adapter that scores every JS/TS repo
  # dross verifies had no gate on any pull request.
  mutation-ts:
    runs-on: ubuntu-latest
    env:
      GOFLAGS: -mod=readonly
      GOPROXY: https://proxy.golang.org,direct
      GOSUMDB: sum.golang.org
      # Turns the test's own skip into a failure. Without it this job is
      # theatre: a runner that lost Node would go green having measured
      # nothing, which is the exact vacuous-pass this repo keeps removing.
      DROSS_REQUIRE_E2E: '1'
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e  # v7.0.0
        with:
          go-version-file: go.mod  # single source: go.mod's toolchain directive
          cache: true

      - name: go version
        run: go version

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4.4.0
        with:
          # Exact patch, not '24' or '24.x' — same reasoning as the Go pin
          # above. A floating version means the largest dependency in this job
          # is the one thing it did not pin.
          node-version: '24.19.0'
          cache: 'npm'
          cache-dependency-path: internal/mutation/testdata/ts-project/package-lock.json

      # --ignore-scripts: install scripts are npm's biggest execution vector,
      # and this tree runs them as root-equivalent on the runner. Verified safe
      # for these deps — esbuild 0.28 ships per-platform binaries as optional
      # dependencies rather than fetching one in a postinstall, and fsevents is
      # macOS-only. `ci`, never `install`: it fails if the lockfile has drifted
      # instead of quietly resolving something else.
      - name: npm ci (fixture)
        working-directory: internal/mutation/testdata/ts-project
        run: npm ci --ignore-scripts

      # No allowlist and no `|| true`: a high-or-critical advisory fails the
      # build and the dependency gets upgraded. --omit=dev is deliberately NOT
      # passed — every dependency here is a devDependency, so omitting them
      # would audit nothing and pass by construction.
      - name: npm audit (fixture)
        working-directory: internal/mutation/testdata/ts-project
        run: npm audit --audit-level=high

      # Both node-gated legs: the adapter's whole-file run, and the
      # construct-range proof (three Stryker runs against the real
      # @babel/parser in the fixture tree — the ±25 window vs the enclosing
      # construct). Selected by name because the rest of the package runs in
      # the `test` job; DROSS_REQUIRE_E2E above turns a skip here into a
      # failure, which only means something for tests this selector reaches.
      - name: stryker end-to-end mutation run
        run: go test -count=1 -run 'TestStrykerRunsEndToEnd|TestConstructRangeRecoversTheDeepMutant|TestBabelParsesGenericsAndJSX|TestRealParserSpansMatchTheExpansion|TestUnresolvableParserIsUnavailable' ./internal/mutation/

  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      # shellcheck is preinstalled on ubuntu-latest runners — no third-party action.
      # Every shipped shell script is linted, not just the installer: release.yml
      # shells out to scripts/release-version.sh to compute the release tag.
      - name: shellcheck
        run: shellcheck install.sh scripts/*.sh
