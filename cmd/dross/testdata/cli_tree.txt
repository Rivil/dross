dross | use="dross" short="Dross — refine intent into verified code"
dross architecture | use="architecture" short="Inspect and repair ARCHITECTURE.md symbol links"
dross architecture check | use="check" short="Report stale ARCHITECTURE.md symbol links; --fix repoints moved ones"
dross architecture check flag --fix (bool) default="false" noopt="true" usage="rewrite moved links' :line in place (unresolved/ambiguous left untouched)"
dross base-branch | use="base-branch" short="Print the branch new phase/quick work should fork off"
dross changes | use="changes" short="Append-only record of files touched per task during execute"
dross changes record | use="record <phase-id> <task-id>" short="Record files (and optionally commit, notes, landmarks) for a task"
dross changes record flag --commit (string) default="" usage="commit SHA for this task"
dross changes record flag --files (string) default="" usage="comma-separated list of files touched"
dross changes record flag --landmark (stringArray) default="[]" usage="typed landmark \"feature=…, symbol=…, loc=file:line, what=…\" (repeatable; values may contain commas — a new pair starts only at a recognised key=)"
dross changes record flag --notes (string) default="" usage="free-form notes"
dross changes show | use="show <phase-id>" short="Print changes.json (or empty record if none yet)"
dross changes show flag --json (bool) default="false" noopt="true" usage="accepted for symmetry; changes show always emits JSON"
dross checkout | use="checkout <branch>" short="Switch to a branch, guarding the live state.json"
dross codex | use="codex <file> [file...]" short="Polyglot code insight — symbols, refs, siblings, recent activity"
dross defaults | use="defaults" short="Manage cross-project defaults at ~/.claude/dross/defaults.toml"
dross defaults save | use="save" short="Save the current project's [remote] as global defaults for future projects"
dross defaults show | use="show" short="Print the global defaults file"
dross defaults show flag --json (bool) default="false" noopt="true" usage="emit the bare JSON document instead of toml (no `# <path>` header)"
dross deferred | use="deferred" short="Inspect and route deferred items across phase specs"
dross deferred add | use="add <text>" short="File a new deferred item (current phase, or the project store when there's no home)"
dross deferred add flag --target (string) default="" usage="route it to a destination phase slug in the same command"
dross deferred add flag --why (string) default="" usage="why this was parked (optional)"
dross deferred dismiss | use="dismiss <phase> <idx>" short="Retire a phase's Nth deferred item to a dismissed state (someday-only; --undo to reverse)"
dross deferred dismiss flag --undo (bool) default="false" noopt="true" usage="clear the dismissed state (back to someday)"
dross deferred list | use="list" short="List deferred items across phase specs (filter + JSON for triage)"
dross deferred list flag --dismissed (bool) default="false" noopt="true" usage="only dismissed items (hidden from every other view)"
dross deferred list flag --json (bool) default="false" noopt="true" usage="emit a JSON array (for prompt consumption)"
dross deferred list flag --milestone (string) default="" usage="only items from phases in this milestone's phases array"
dross deferred list flag --routed (bool) default="false" noopt="true" usage="only routed (target set) items"
dross deferred list flag --someday (bool) default="false" noopt="true" usage="only unrouted (no target, not dismissed) items"
dross deferred list flag --target (string) default="" usage="only items routed to this phase slug"
dross deferred route | use="route <phase> <idx>" short="Stamp a target on a phase's Nth deferred item"
dross deferred route flag --target (string) default="" usage="destination phase slug to stamp (required)"
dross deferred unroute | use="unroute <phase> <idx>" short="Clear a routed deferred item's target, returning it to someday"
dross doctor | use="doctor" short="Check project-level health (.dross/project.toml vs reality)"
dross env | use="env" short="Manage env keys in ~/.claude/settings.json (values never displayed)"
dross env list | use="list" short="List env keys present in ~/.claude/settings.json (values masked)"
dross env set | use="set <KEY>" short="Prompt for value (hidden input) and write to settings.json"
dross env unset | use="unset <KEY>" short="Remove a key from settings.json env"
dross hooks | use="hooks" short="Manage the dross-owned Claude Code hooks"
dross hooks ensure | use="ensure" short="Idempotently wire the dross PreCompact + SessionStart hooks into user-level settings.json"
dross init | use="init" short="Bootstrap .dross/ in the current repo (greenfield)"
dross init flag --force (bool) default="false" noopt="true" usage="remove existing .dross/ before init"
dross install | use="install" short="Install dross slash commands + prompts into ~/.claude"
dross install flag --copy (bool) default="false" noopt="true" usage="write real-file copies from the embedded assets (end-user install)"
dross install flag --link (bool) default="false" noopt="true" usage="symlink the source assets/ for live dev edits (requires a source checkout)"
dross install flag --no-statusline (bool) default="false" noopt="true" usage="un-wire the dross statusline from ~/.claude/settings.json"
dross install flag --statusline (bool) default="false" noopt="true" usage="also wire the dross statusline into ~/.claude/settings.json"
dross interaction | use="interaction" short="The interaction playbook injected into interactive command prompts"
dross interaction show | use="show" short="Render the interaction playbook for prompt injection"
dross issue | use="issue" short="Mirror planning onto the issue board and pull inbound issues"
dross issue backlog | use="backlog" short="Mirror the milestone backlog onto the board"
dross issue backlog sync | use="sync [version]" short="Sync the milestone backlog (unscaffolded slugs + someday ideas) to the board"
dross issue backlog sync flag --phase (string) default="" usage="resolve the version from this phase's spec.toml [phase].milestone (use at ship finalize)"
dross issue disable | use="disable" short="Turn off issue-board sync for this project"
dross issue dismiss | use="dismiss <issue-id>" short="Stop an inbound issue from resurfacing in triage"
dross issue enable | use="enable" short="Turn on issue-board sync for this project"
dross issue link | use="link <phase-id> <issue-id>" short="Adopt an existing board issue as a phase's tracking issue"
dross issue list | use="list" short="Show the current artefact<->issue links from board.json"
dross issue milestone | use="milestone" short="Mirror a milestone onto the board"
dross issue milestone sync | use="sync <version>" short="Ensure a board milestone exists for a dross milestone"
dross issue milestone sync flag --close (bool) default="false" noopt="true" usage="resolve the milestone's epic (use at milestone finalize; epic mode only)"
dross issue phase | use="phase" short="Mirror a phase onto the board"
dross issue phase sync | use="sync <phase-id>" short="Create or update the board issue for a phase (idempotent)"
dross issue phase sync flag --close (bool) default="false" noopt="true" usage="close the issue (use on ship)"
dross issue phase sync flag --status (string) default="" usage="lifecycle status label (planned | in-progress | shipped | complete | task-in-progress | task-in-review | task-complete | uat); derived from the plan if unset"
dross issue pull | use="pull" short="List open board issues not yet linked to dross work (inbound triage)"
dross issue pull flag --json (bool) default="false" noopt="true" usage="emit a JSON envelope {issues, error} (for prompt consumption)"
dross issue pull flag --labels (string) default="" usage="only issues with these labels (csv, e.g. bug,enhancement)"
dross issue pull flag --mark (bool) default="false" noopt="true" usage="record the pull time in board.json (otherwise read-only)"
dross issue pull flag --state (string) default="open" usage="issue state: open|closed|all"
dross issue quick | use="quick <ref> [title]" short="Open (or --close) a standalone issue for a quick task"
dross issue quick flag --close (bool) default="false" noopt="true" usage="close the quick issue linked to <ref>"
dross issue reap | use="reap" short="Close board mirrors the forward lifecycle left stranded"
dross issue reap flag --apply (bool) default="false" noopt="true" usage="write the plan to the board (default: dry run)"
dross issue reap flag --namespace (stringArray) default="[]" usage="limit the sweep to one mirror namespace (repeatable; default: every namespace)"
dross issue reap flag --undo (bool) default="false" noopt="true" usage="restore the cards the last applied run closed, to the exact state each held before it"
dross issue task | use="task" short="Mirror plan tasks onto the board, and pull board moves back"
dross issue task pull | use="pull [phase-id]" short="Apply board task-state moves back into plan.toml"
dross issue task pull flag --apply (bool) default="false" noopt="true" usage="write the moves into plan.toml (default: report only)"
dross issue task sync | use="sync <phase-id> [task-id]" short="Mirror a phase's plan tasks as issues, one per task"
dross issue task sync flag --close (bool) default="false" noopt="true" usage="resolve each synced task's issue (use at ship finalize; requires --status)"
dross issue task sync flag --status (string) default="" usage="lifecycle status to drive the task's state (planned | in-progress | shipped | complete | task-in-progress | task-in-review | task-complete | uat)"
dross local | use="local" short="Read and write .dross/local.toml (gitignored, machine-local values)"
dross local get | use="get <key>" short="Print a machine-local value (empty when unset)"
dross local set | use="set <key> <value>" short="Write a machine-local value to .dross/local.toml"
dross milestone | use="milestone" short="Manage milestones under .dross/milestones/"
dross milestone add | use="add [version] <list.path> <value>" short="Append a value to a list field (version defaults to state.current_milestone)"
dross milestone complete | use="complete [version]" short="Open the milestone->main PR, or (--finalize) ff main and delete the milestone branch after merge"
dross milestone complete flag --finalize (bool) default="false" noopt="true" usage="after the milestone PR merges: ff main from origin and delete milestone/<version> local+remote"
dross milestone complete flag --force-unverified (bool) default="false" noopt="true" usage="skip the 'every phase in the milestone must verify pass' gate"
dross milestone create | use="create <version>" short="Create a new milestone (e.g. v0.1)"
dross milestone create flag --base (string) default="" usage="force the branch to cut from (default: the current milestone's branch while unmerged, else the main branch)"
dross milestone get | use="get [version] <dotted.path>..." short="Read one or more milestone fields by dotted path (version defaults to state.current_milestone)"
dross milestone list | use="list" short="List milestones"
dross milestone progress | use="progress [version]" short="Report how far a milestone has got: status, done/total, and what is left"
dross milestone progress flag --json (bool) default="false" noopt="true" usage="emit the bare JSON document instead of toml (no `# <path>` header)"
dross milestone prune | use="prune" short="Delete milestone/* branches whose work is already on the main branch"
dross milestone prune flag --dry-run (bool) default="false" noopt="true" usage="list what would be deleted and exit without deleting anything"
dross milestone prune flag --yes (bool) default="false" noopt="true" usage="skip the confirmation prompt (for scripted use)"
dross milestone remove | use="remove [version] <list.path> <value>" short="Remove an exact value from a list field (version defaults to state.current_milestone)"
dross milestone replace | use="replace [version] <list.path> <old> <new>" short="Replace an exact value in a list field, keeping its position"
dross milestone set | use="set [version] <dotted.path> <value>" short="Write a single scalar milestone field (version defaults to state.current_milestone)"
dross milestone show | use="show [version]" short="Print a milestone toml (defaults to state.current_milestone)"
dross milestone show flag --json (bool) default="false" noopt="true" usage="emit the bare JSON document instead of toml (no `# <path>` header)"
dross mutation | use="mutation" short="Configure how mutation testing runs"
dross mutation remote | use="remote" short="Deprecated: use `dross remote` — authorize a host dross runs this repo's code on"
dross mutation remote bootstrap | use="bootstrap" short="Install the mutation and lane toolchains the granted host is missing"
dross mutation remote bootstrap flag --apply (bool) default="false" noopt="true" usage="actually install (default is a dry run that changes nothing)"
dross mutation remote grant | use="grant <host> <workdir>" short="Authorize remote runs to execute on <host> under <workdir>"
dross mutation remote grant flag --add (bool) default="false" noopt="true" usage="authorize an ADDITIONAL host, keeping the existing grant; runs take the first that answers"
dross mutation remote revoke | use="revoke" short="Withdraw the remote authorization"
dross mutation remote status | use="status" short="Print the remote this machine has authorized, if any"
dross onboard | use="onboard" short="Adopt the existing repo into dross by scanning signal files"
dross onboard flag --force (bool) default="false" noopt="true" usage="remove existing .dross/ before onboard"
dross pause | use="pause" short="Handoff helpers — `--auto` merges a mechanical snapshot into .dross/handoff.md"
dross pause flag --auto (bool) default="false" noopt="true" usage="non-interactive: write the mechanical snapshot section of .dross/handoff.md (PreCompact hook target)"
dross phase | use="phase" short="Manage phase directories under .dross/phases/"
dross phase backfill | use="backfill" short="Mark legacy phases complete from ship-commit evidence (preview unless --apply)"
dross phase backfill flag --apply (bool) default="false" noopt="true" usage="write the inferred completion markers (default: preview only)"
dross phase checkout | use="checkout <phase-id>" short="Switch to phase/<id>, guarding the live state.json"
dross phase complete | use="complete [phase-id]" short="Finalize a phase after squash-merge: ff the reconcile branch, delete phase/<id>"
dross phase complete flag --base (string) default="" usage="the branch this phase was forked from, when no base is recorded (a pre-check phase, or one created with --no-branch)"
dross phase complete flag --recover (bool) default="false" noopt="true" usage="on a diverged base (main or milestone/<version>), reset to origin and restore .dross/ in one shot instead of aborting"
dross phase create | use="create <title>" short="Create the next phase directory and switch to phase/<id>"
dross phase create flag --adopt (bool) default="false" noopt="true" usage="take over a slug that has already been started (refused without this)"
dross phase create flag --no-branch (bool) default="false" noopt="true" usage="skip creating/checking out the phase/<id> git branch (advanced)"
dross phase insert | use="insert <title>" short="Scaffold a phase and place it at a position in the milestone array"
dross phase insert flag --after (string) default="" usage="place the new phase immediately after this phase slug"
dross phase insert flag --before (string) default="" usage="place the new phase immediately before this phase slug"
dross phase list | use="list" short="List phases, marking the done ones"
dross phase list flag --milestone (string) default="" usage="list this milestone's roadmap in array order, including slugs with no phase directory"
dross phase migrate | use="migrate" short="Convert legacy NN-slug phases to bare-slug identity"
dross phase move | use="move <slug>" short="Reorder a phase within its milestone's phases array"
dross phase move flag --after (string) default="" usage="place <slug> immediately after this phase slug"
dross phase move flag --before (string) default="" usage="place <slug> immediately before this phase slug"
dross phase number | use="number <phase-id>" short="Print a phase's 1-based ordinal within its milestone"
dross phase reconcile | use="reconcile" short="Complete every merged-but-uncompleted phase"
dross phase red-proof | use="red-proof" short="Record the commit a phase's red proof is pinned to"
dross phase red-proof repoint | use="repoint [phase-id]" short="Repair red-proof pins whose commit origin can no longer reach"
dross phase red-proof repoint flag --apply (bool) default="false" noopt="true" usage="write the repairs; without it the run only reports what it would do"
dross phase red-proof set | use="set <phase-id>" short="Pin a phase's red proof to a commit and the doc that replays it"
dross phase red-proof set flag --doc (string) default="" usage="repo-relative path of the doc that replays the proof"
dross phase red-proof set flag --replay (string) default="" usage="command that replays the proof, run at the proposed commit when the pin is repointed"
dross phase red-proof set flag --sha (string) default="" usage="the commit the red proof was captured at"
dross phase rename | use="rename <old-slug> <new-slug>" short="Rename a phase: directory, spec id, milestone entry, deferred targets, branch"
dross phase show | use="show <phase-id>" short="Print the spec.toml and plan.toml for a phase"
dross phase show flag --json (bool) default="false" noopt="true" usage="emit the bare JSON document instead of toml (no `# <path>` header)"
dross profile | use="profile" short="Read user profile (global + project overrides)"
dross profile seed | use="seed" short="Seed the global profile from GSD's USER-PROFILE.md if present"
dross profile show | use="show" short="Print profile (global | project | merged)"
dross profile show flag --json (bool) default="false" noopt="true" usage="emit the bare JSON document instead of toml (no `# <path>` header)"
dross profile show flag --scope (string) default="merged" usage="global | project | merged"
dross project | use="project" short="Read and edit .dross/project.toml"
dross project get | use="get <dotted.path>..." short="Print one or more fields by dotted path (multiple emit a keyed JSON object)"
dross project set | use="set <dotted.path> <value>" short="Write a single field by dotted path (--unset clears it)"
dross project set flag --unset (bool) default="false" noopt="true" usage="clear the field at <dotted.path> instead of writing a value"
dross project show | use="show" short="Print project.toml"
dross project show flag --json (bool) default="false" noopt="true" usage="emit the bare JSON document instead of toml (no `# <path>` header)"
dross quality | use="quality" short="Deterministic surface of the dross-quality audit (run dirs, analyzer detection, scaffold)"
dross quality detect | use="detect [path]" short="Detect languages and report installed vs missing analyzers"
dross quality findings | use="findings [id]" short="List tracked findings, set a finding's state, or reconcile a run against prior state"
dross quality findings flag --state (string) default="" usage="new lifecycle state: tracked|resolved|dismissed"
dross quality findings list | use="list" short="List tracked findings and their current lifecycle state across runs"
dross quality findings reconcile | use="reconcile <run-dir>" short="Fold a completed run's findings against prior durable state (strictly post-scan)"
dross quality run | use="run [path]" short="Create a quality run dir (.dross/quality/<id>) and write the tool-coverage manifest"
dross quality scaffold | use="scaffold <run-dir>" short="Build a proposed remediation spec.toml from a run's findings.toml ledger"
dross quality scaffold flag --phase-id (string) default="07-remediate-quality" usage="phase id for the scaffolded spec"
dross quality scaffold flag --title (string) default="Remediate quality findings" usage="title for the scaffolded spec"
dross reentry | use="reentry" short="One-line 'you are here + next command' for a fresh session (SessionStart hook target)"
dross remote | use="remote" short="Authorize (or inspect) a host that dross runs this repo's code on"
dross remote bootstrap | use="bootstrap" short="Install the mutation and lane toolchains the granted host is missing"
dross remote bootstrap flag --apply (bool) default="false" noopt="true" usage="actually install (default is a dry run that changes nothing)"
dross remote grant | use="grant <host> <workdir>" short="Authorize remote runs to execute on <host> under <workdir>"
dross remote grant flag --add (bool) default="false" noopt="true" usage="authorize an ADDITIONAL host, keeping the existing grant; runs take the first that answers"
dross remote revoke | use="revoke" short="Withdraw the remote authorization"
dross remote status | use="status" short="Print the remote this machine has authorized, if any"
dross repair | use="repair" short="Detect and restore clobbered or missing tracked .dross/ files"
dross repair flag --apply (bool) default="false" noopt="true" usage="write the restores and commit (default: dry-run report only)"
dross rule | use="rule" short="Manage two-tier rules (global + project)"
dross rule add | use="add <text>" short="Add a rule"
dross rule add flag --id (string) default="" usage="rule id (auto-generated if empty)"
dross rule add flag --scope (string) default="project" usage="global | project"
dross rule add flag --severity (string) default="hard" usage="hard | soft"
dross rule disable | use="disable <id>" short="disable a rule (keeps it in file)"
dross rule disable flag --scope (string) default="project" usage="global | project"
dross rule enable | use="enable <id>" short="enable a rule (keeps it in file)"
dross rule enable flag --scope (string) default="project" usage="global | project"
dross rule list | use="list" short="List rules in a scope (or merged)"
dross rule list flag --merged (bool) default="false" noopt="true" usage="show merged + rendered prompt block"
dross rule list flag --scope (string) default="project" usage="global | project | all"
dross rule promote | use="promote <id>" short="Move a project rule to global scope"
dross rule remove | use="remove <id>" short="Remove a rule by id"
dross rule remove flag --scope (string) default="project" usage="global | project"
dross rule show | use="show" short="Render merged rules as the <rules> prompt block"
dross run | use="run [name] [args...]" short="Run a configured [runtime] command"
dross security | use="security" short="Deterministic surface of the dross-secure audit (run dirs, scanner detection, scaffold)"
dross security detect | use="detect [path]" short="Detect languages and report installed vs missing scanners"
dross security findings | use="findings [id]" short="List tracked findings, set a finding's state, or reconcile a run against prior state"
dross security findings flag --state (string) default="" usage="new lifecycle state: tracked|resolved|dismissed"
dross security findings list | use="list" short="List tracked findings and their current lifecycle state across runs"
dross security findings reconcile | use="reconcile <run-dir>" short="Fold a completed run's findings against prior durable state (strictly post-scan)"
dross security run | use="run [path]" short="Create a security run dir (.dross/security/<id>) and write the tool-coverage manifest"
dross security run flag --image (string) default="" usage="container image ref for dockle (dross never builds; empty → skipped-with-reason). Falls back to $DROSS_IMAGE."
dross security scaffold | use="scaffold <run-dir>" short="Build a proposed remediation spec.toml from a run's findings.toml ledger"
dross security scaffold flag --phase-id (string) default="06-remediate-security" usage="phase id for the scaffolded spec"
dross security scaffold flag --title (string) default="Remediate security findings" usage="title for the scaffolded spec"
dross ship | use="ship [phase-id]" short="Push phase/<id> and open a PR via the project's provider"
dross ship flag --auto (bool) default="false" noopt="true" usage="non-interactive: request zero reviewers for this run (without mutating remote.reviewers) and use the generated body; for scripts and loops"
dross ship flag --body (string) default="" usage="PR body override (overrides generated body)"
dross ship flag --body-file (string) default="" usage="read PR body from file"
dross ship flag --draft (bool) default="false" noopt="true" usage="open the PR as draft"
dross ship flag --force (bool) default="false" noopt="true" usage="force-with-lease the push (use when re-pushing after rewriting phase/<id>)"
dross ship flag --force-unverified (bool) default="false" noopt="true" usage="skip the 'verify must be pass' gate"
dross ship flag --json (bool) default="false" noopt="true" usage="emit a single JSON object {url, number, result} on stdout and suppress human narration; composable with --auto for scripts and loops"
dross ship flag --no-push (bool) default="false" noopt="true" usage="don't push the phase branch or open a PR"
dross ship flag --print-body (bool) default="false" noopt="true" usage="print the generated PR body and exit (no push, no PR)"
dross ship flag --title (string) default="" usage="PR title (default: 'phase <id>: <spec title>')"
dross ship comment | use="comment --pr <n> (--body \"...\" | --body-file <path>)" short="Post a comment to a PR via the project's provider"
dross ship comment flag --body (string) default="" usage="comment body (markdown)"
dross ship comment flag --body-file (string) default="" usage="read comment body from file"
dross ship comment flag --pr (int) default="0" usage="PR number to comment on (required)"
dross ship recover | use="recover [phase-id]" short="Heal main after a legacy squash-merge (one-shot migration)"
dross ship recover flag --pre-merge-sha (string) default="" usage="commit holding the pre-merge .dross/ tree (default: current HEAD)"
dross stack | use="stack" short="Stack profiles: detect the stack and tune runtime/tools/loadout to it"
dross stack apply | use="apply" short="Re-sync project.toml [runtime] from the current stack profile"
dross stack detect | use="detect [path]" short="Detect the stack at path and print the matched profile id (or 'unsupported')"
dross stack list | use="list" short="List available stack profiles (embedded + user dir)"
dross stack loadout | use="loadout [id]" short="Emit the agent loadout for the stack as a markdown block"
dross stack show | use="show <id>" short="Print a stack profile by id"
dross stack show flag --json (bool) default="false" noopt="true" usage="emit the bare JSON document instead of toml (no `# <path>` header)"
dross state | use="state" short="Read and edit .dross/state.json"
dross state bump | use="bump <segment>" short="Bump a version segment (currently only `internal`)"
dross state get | use="get <field>..." short="Print one or more state fields (multiple emit a keyed JSON object)"
dross state set | use="set <field> <value>" short="Set version | current_milestone | current_phase | current_phase_status"
dross state show | use="show" short="Print state.json"
dross state show flag --json (bool) default="false" noopt="true" usage="accepted for symmetry; state show always emits JSON"
dross state touch | use="touch <action>" short="Append an activity entry"
dross stats | use="stats" short="Inspect local usage telemetry recorded at ~/.claude/dross/telemetry.jsonl"
dross stats opt-in | use="opt-in" short="Enable telemetry recording"
dross stats opt-out | use="opt-out" short="Disable telemetry recording"
dross stats path | use="path" short="Print the telemetry file path"
dross stats show | use="show" short="Print aggregate views of recorded events"
dross stats show flag --json (bool) default="false" noopt="true" usage="emit the bare JSON document instead of toml (no `# <path>` header)"
dross stats show flag --since (string) default="" usage="filter to events newer than (e.g. 7d, 24h, 2026-05-01)"
dross status | use="status" short="Where am I — project, milestone, phase, last activity, next task"
dross statusline | use="statusline" short="Render the Claude Code status line (reads status JSON on stdin)"
dross statusline disable | use="disable" short="Un-wire the dross statusline from ~/.claude/settings.json"
dross statusline enable | use="enable" short="Wire the dross statusline into ~/.claude/settings.json"
dross survivor | use="survivor" short="Accept, route and list surviving mutants (the drain for out-of-diff debt)"
dross survivor accept | use="accept <file>:<line>" short="Record a survivor as permanently accepted, with the reason why"
dross survivor accept flag --category (string) default="" usage="shared reason name; pass --reason once alongside it to define the prose"
dross survivor accept flag --op (string) default="" usage="mutation operator, e.g. CONDITIONALS_NEGATION (required)"
dross survivor accept flag --reason (string) default="" usage="why this survivor is permanently acceptable"
dross survivor drain | use="drain" short="Report every survivor with no disposition, across whole packages (no diff scoping)"
dross survivor drain flag --packages (stringSlice) default="[]" usage="Go packages to drain (default: every package in the repo)"
dross survivor drain flag --phase (string) default="" usage="phase being drained; a survivor routed here counts outstanding (default: current phase)"
dross survivor drain flag --report (stringSlice) default="[]" usage="classify a recorded gremlins JSON report instead of running the adapter (repeatable)"
dross survivor list | use="list" short="List accepted survivors (--stale for those whose subject is gone)"
dross survivor list flag --json (bool) default="false" noopt="true" usage="emit the bare JSON document instead of toml (no `# <path>` header)"
dross survivor list flag --stale (bool) default="false" noopt="true" usage="only acceptances whose subject no longer exists"
dross survivor retire | use="retire <key>..." short="Remove acceptances from the store (--stale for every acceptance whose subject is gone)"
dross survivor retire flag --stale (bool) default="false" noopt="true" usage="retire every acceptance whose subject no longer exists"
dross survivor route | use="route <file>:<line>" short="Route a survivor to a destination phase as a deferred item"
dross survivor route flag --op (string) default="" usage="mutation operator, e.g. CONDITIONALS_NEGATION (required)"
dross survivor route flag --target (string) default="" usage="destination phase slug (required)"
dross task | use="task" short="Inspect and update tasks within a phase plan"
dross task add | use="add <phase-id>" short="Append or insert a new task into a phase plan"
dross task add flag --after (string) default="" usage="insert immediately after this task id"
dross task add flag --before (string) default="" usage="insert immediately before this task id"
dross task add flag --covers (stringSlice) default="[]" usage="criterion ids this task covers (comma-separated)"
dross task add flag --depends-on (stringSlice) default="[]" usage="task ids this task depends on (comma-separated)"
dross task add flag --description (string) default="" usage="task description"
dross task add flag --files (stringSlice) default="[]" usage="files this task touches (comma-separated)"
dross task add flag --test-contract (stringArray) default="[]" usage="one \"if X breaks, TestY fails\" statement (repeatable; not split on commas)"
dross task add flag --title (string) default="" usage="task title (required)"
dross task add flag --wave (int) default="0" usage="explicit wave (default: derived from --depends-on)"
dross task edit | use="edit <phase-id> <task-id>" short="Update an existing task's fields (partial; status not editable)"
dross task edit flag --add-test-contract (stringArray) default="[]" usage="append one statement to the existing test contract (repeatable; not split on commas)"
dross task edit flag --covers (stringSlice) default="[]" usage="replace covered criterion ids (comma-separated)"
dross task edit flag --depends-on (stringSlice) default="[]" usage="replace depends_on task ids (comma-separated)"
dross task edit flag --description (string) default="" usage="new task description"
dross task edit flag --files (stringSlice) default="[]" usage="replace the files this task touches (comma-separated)"
dross task edit flag --test-contract (stringArray) default="[]" usage="replace the whole test contract (repeatable; not split on commas)"
dross task edit flag --title (string) default="" usage="new task title"
dross task edit flag --wave (int) default="0" usage="new wave"
dross task list | use="list [phase-id]" short="List every task in a phase plan (id, wave, status, title)"
dross task list flag --json (bool) default="false" noopt="true" usage="emit the task array as JSON (for prompt consumption)"
dross task move | use="move <phase-id> <task-id>" short="Reposition a task relative to another (--after/--before)"
dross task move flag --after (string) default="" usage="move immediately after this task id"
dross task move flag --before (string) default="" usage="move immediately before this task id"
dross task next | use="next <phase-id>" short="Print the id of the next runnable task (or nothing if none)"
dross task remove | use="remove <phase-id> <task-id>" short="Remove a task from a phase plan (dependency-safe)"
dross task remove flag --force (bool) default="false" noopt="true" usage="strip the removed id from dependents' depends_on instead of refusing"
dross task show | use="show <phase-id> <task-id>" short="Print one task's record from plan.toml"
dross task show flag --json (bool) default="false" noopt="true" usage="emit the bare JSON document instead of toml (no `# <path>` header)"
dross task status | use="status <phase-id> <task-id> <pending|in_progress|done|failed>" short="Set a task's status in plan.toml"
dross techdebt | use="techdebt" short="Scan tracked files for tech-debt markers + size heuristics (.dross/techdebt/<id>)"
dross test | use="test [selector...]" short="Run this repo's test suite"
dross test flag --files (stringArray) default="[]" usage="repo-relative path to resolve against the declared test lanes (repeatable)"
dross test flag --local (bool) default="false" noopt="true" usage="run on this machine even when a remote is granted"
dross test flag --wait (string) default="10m0s" usage="how long to wait for a host held by a mutation leg before running alongside it (0 = at once)"
dross test lane | use="lane" short="Declare, list, edit and remove [[runtime.test_lane]] entries"
dross test lane add | use="add <name>" short="Declare a test lane"
dross test lane add flag --command (string) default="" usage="the command line this lane runs"
dross test lane add flag --empty-exit (intSlice) default="[]" usage="exit code this lane's runner uses for \"collected no tests\" (repeatable); requires --selector"
dross test lane add flag --install (string) default="" usage="optional line that installs this lane's toolchain; replaces dross's built-in recipe and carries its own consent grant, separate from the lane's"
dross test lane add flag --match (stringArray) default="[]" usage="glob this lane matches (repeatable)"
dross test lane add flag --prepare (string) default="" usage="optional bootstrap line run before this lane's command, on the same host; covered by the same consent grant"
dross test lane add flag --selector (string) default="" usage="shape the matched paths take when appended to the command (path | dir | go-package); omitted runs the command untouched"
dross test lane add flag --selector-join (string) default="" usage="join a {paths} expansion into ONE argument with this separator (\"|\" for `ctest -R`); omitted expands to separate arguments"
dross test lane add flag --selector-template (string) default="" usage="where the matched paths land on the command line, for a runner the selector styles cannot shape; {path} repeats the template per path, {paths} substitutes them into one instance. Requires --selector"
dross test lane add flag --toolchain (stringArray) default="[]" usage="binary this lane needs on the host that runs it (repeatable); omitted derives it from the first token of --command and --prepare"
dross test lane edit | use="edit <name>" short="Change a declared lane's fields in place, keeping its grant"
dross test lane edit flag --command (string) default="" usage="the command line this lane runs"
dross test lane edit flag --empty-exit (stringArray) default="[]" usage="exit code this lane's runner uses for \"collected no tests\" (repeatable); replaces the declared list, pass \"\" to clear it"
dross test lane edit flag --install (string) default="" usage="the line that installs this lane's toolchain; replaces dross's built-in recipe, pass \"\" to drop it"
dross test lane edit flag --match (stringArray) default="[]" usage="glob this lane matches (repeatable); replaces the declared list, pass \"\" to clear it"
dross test lane edit flag --prepare (string) default="" usage="the lane's bootstrap line; pass \"\" to clear it"
dross test lane edit flag --selector (string) default="" usage="shape the matched paths take (path | dir | go-package); pass \"\" to clear it"
dross test lane edit flag --selector-join (string) default="" usage="join a {paths} expansion into ONE argument with this separator; pass \"\" to clear it"
dross test lane edit flag --selector-template (string) default="" usage="where the matched paths land on the command line; {path} repeats the template per path, {paths} substitutes them into one instance. Pass \"\" to clear it"
dross test lane edit flag --toolchain (stringArray) default="[]" usage="binary this lane needs on the host that runs it (repeatable); replaces the derived list, pass \"\" to go back to deriving it"
dross test lane install | use="install <name>" short="Install the toolchain one test lane is missing, on the machine that is missing it"
dross test lane install flag --apply (bool) default="false" noopt="true" usage="actually install (default is a dry run that changes nothing)"
dross test lane install flag --local (bool) default="false" noopt="true" usage="install on this machine even when the granted host is the side with the gap"
dross test lane list | use="list" short="Print the declared test lanes"
dross test lane preview | use="preview [paths...]" short="Show the command line each lane would spawn for a file set, without running anything"
dross test lane preview flag --files (stringArray) default="[]" usage="repo-relative path to resolve against the declared lanes (repeatable)"
dross test lane preview flag --json (bool) default="false" noopt="true" usage="emit the preview as a bare JSON document instead of the transcript"
dross test lane preview flag --lane (string) default="" usage="narrow the preview to one declared lane"
dross test lane preview flag --no-probe (bool) default="false" noopt="true" usage="do not contact the granted host; report its locality as unresolved"
dross test lane remove | use="remove <name>" short="Delete a test lane and drop its consent grant"
dross trust | use="trust" short="Consent to dross running this repo's runtime.test_command on this machine"
dross trust flag --check (bool) default="false" noopt="true" usage="exit 0 if consent is current, non-zero otherwise; prints nothing on success"
dross trust flag --lane (string) default="" usage="grant consent for the named [[runtime.test_lane]]'s command instead of runtime.test_command"
dross trust flag --lane-install (string) default="" usage="grant consent for the named [[runtime.test_lane]]'s install line — a separate grant from --lane, because installing is a different act from running a suite"
dross trust flag --replay (string) default="" usage="grant consent for <phase-id>'s recorded red-proof replay command instead of runtime.test_command"
dross trust flag --run (string) default="" usage="grant consent for `dross run <name>`'s configured command instead of runtime.test_command"
dross update | use="update" short="Update dross to the latest GitHub release"
dross update flag --api-base (string) default="" hidden usage="override the GitHub API base URL (testing)"
dross update flag --check (bool) default="false" noopt="true" usage="report the available version without updating"
dross update flag --force (bool) default="false" noopt="true" usage="reinstall the latest release even if it is not newer"
dross validate | use="validate" short="Schema-check every dross artefact"
dross verify | use="verify <phase-id>" short="Run mutation testing per language and write tests.json + verify.toml skeleton"
dross verify flag --at (string) default="" usage="with --detach, start the run at HH:MM (next occurrence) or an RFC3339 instant, on the host's clock"
dross verify flag --base (string) default="" usage="diff from this commit instead of the resolved fork point (a merged phase falls back to changes.json's base_commit automatically; use this when it has none)"
dross verify flag --detach (bool) default="false" noopt="true" usage="start the run on the granted host and return immediately; collect it later with `dross verify results <phase>`"
dross verify flag --no-wait (bool) default="false" noopt="true" usage="refuse (exit 15, naming the holder) instead of waiting when another run holds the granted host"
dross verify flag --reuse-report (bool) default="false" noopt="true" usage="stryker only: parse the report already on disk instead of launching a run (path + mtime are printed; other legs still run)"
dross verify flag --skip-mutation (bool) default="false" noopt="true" usage="do not run mutation tests (record what would have been mutated, skip execution)"
dross verify finalize | use="finalize <phase-id>" short="Record the resolved verdict from verify.toml as a telemetry outcome event"
dross verify results | use="results <phase-id>" short="Collect a detached mutation run and write tests.json + verify.toml"
dross verify results flag --base (string) default="" usage="diff from this commit instead of the resolved fork point; pass the same value the run was dispatched with"
dross verify scope | use="scope <phase-id>" short="Print the last verify run's range provenance: in-scope files, raw hunks, and per leg the effective ranges or whole-file fallback"
dross verify scope flag --json (bool) default="false" noopt="true" usage="emit the provenance record as JSON — the same values, nothing re-rendered"
dross verify status | use="status" short="List detached mutation runs this machine dispatched"
dross verify status flag --cancel (string) default="" usage="cancel the named phase's detached run: kill it on the host and drop the record"
dross version | use="version" short="Print version, commit, and build date"
dross watch | use="watch" short="Read-only digest of board inbound + phase drift since the last tick"
dross watch flag --json (bool) default="false" noopt="true" usage="emit the digest as JSON (for /dross-watch)"
